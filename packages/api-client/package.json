{
  "name": "@plexica/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Shared typed HTTP client for the Plexica platform — tenant-scoped and admin clients with auth interceptors",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "require": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "@plexica/types": "workspace:*",
    "axios": "^1.13.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "axios-mock-adapter": "^2.1.0",
    "typescript": "^5.9.3",
    "vitest": "^1.2.0"
  }
}
