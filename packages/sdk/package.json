{
  "name": "@plexica/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Plugin SDK for the Plexica platform — base classes, API client, event client, and service helpers",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "require": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "@plexica/event-bus": "workspace:*",
    "@plexica/types": "workspace:*",
    "@plexica/ui": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
