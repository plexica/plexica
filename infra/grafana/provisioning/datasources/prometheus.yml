# Prometheus datasource — auto-provisioned in Grafana
# Architecture §7.2: Prometheus metrics visualization

apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: false
    editable: false
