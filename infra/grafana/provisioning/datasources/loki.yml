# Loki datasource — auto-provisioned in Grafana
# ADR-022 Decision 3

apiVersion: 1
datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
