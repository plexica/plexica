{
  "clientId": "plexica-admin",
  "name": "Plexica Admin App",
  "description": "Public client for the super-admin app. Uses Authorization Code Flow with PKCE (S256) against the master realm. DirectAccessGrants disabled — all authentication goes through the browser PKCE flow.",
  "enabled": true,
  "protocol": "openid-connect",
  "publicClient": true,
  "standardFlowEnabled": true,
  "implicitFlowEnabled": false,
  "directAccessGrantsEnabled": false,
  "serviceAccountsEnabled": false,
  "authorizationServicesEnabled": false,
  "bearerOnly": false,
  "frontchannelLogout": false,
  "fullScopeAllowed": true,
  "redirectUris": ["http://localhost:3002/*"],
  "webOrigins": ["http://localhost:3002"],
  "attributes": {
    "access.token.lifespan": "3600",
    "client_credentials.use_refresh_token": "false",
    "use.refresh.tokens": "true"
  },
  "roles": [
    {
      "name": "super_admin",
      "description": "Super administrator with full platform access (master realm only)"
    }
  ]
}
