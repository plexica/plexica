name: Deploy to Production

on:
  push:
    tags:
      - 'v*.*.*'

# Minimal read-only default. Docker Hub push uses DOCKER_USERNAME/DOCKER_PASSWORD
# secrets (not packages: write — this workflow does not push to GHCR).
# Spec 015 T015-09, FR-021.
permissions:
  contents: read

jobs:
  build-and-push:
    name: Build and Push Docker Images
    runs-on: ubuntu-latest
    strategy:
      matrix:
        app:
          - core-api
          - web
          - super-admin
    steps:
      - uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract version from tag
        id: extract_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./apps/${{ matrix.app }}/Dockerfile
          push: true
          tags: |
            plexica/${{ matrix.app }}:${{ steps.extract_version.outputs.VERSION }}
            plexica/${{ matrix.app }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # deploy:
  #   name: Deploy to Kubernetes
  #   runs-on: ubuntu-latest
  #   needs: build-and-push
  #   steps:
  #     - uses: actions/checkout@v6
  #
  #     - name: Install kubectl
  #       uses: azure/setup-kubectl@v3
  #
  #     - name: Configure kubectl
  #       run: |
  #         echo "${{ secrets.KUBECONFIG }}" > kubeconfig
  #         export KUBECONFIG=kubeconfig
  #
  #     - name: Deploy with Helm
  #       run: |
  #         helm upgrade --install plexica ./infrastructure/helm/plexica \
  #           --namespace plexica \
  #           --create-namespace \
  #           --set image.tag=${{ steps.extract_version.outputs.VERSION }}
