# Design Specification: Workspace Management

> **Spec reference**: `.forge/specs/009-workspace-management/spec.md`
> **User journeys**: `.forge/specs/009-workspace-management/user-journey.md`
> **Design system**: `.forge/ux/design-system.md`
> **Generated by**: `/forge-ux`
> **Date**: 2026-03-02
> **Status**: Draft

---

## 1. Overview

### 1.1 Scope

This design specification covers the UX/UI for the Workspace Management
feature (Spec 009). It documents 4 key screens:

1. **Workspace Switcher** — dropdown for switching between workspaces (✅ implemented, documenting as-built + improvements)
2. **Workspace Settings** — tabbed settings page with typed form for Gap 4 (🟡 partial, needs typed settings form)
3. **Resource Sharing UI** — share dialog, shared resources list, revoke flow for Gap 3 (🔴 not implemented)
4. **Member Management** — member list, role editor, add/remove dialogs (✅ implemented, documenting as-built + improvements)

### 1.2 Functional Requirements Covered

| FR Reference | Description                       | Screen(s)                          |
| ------------ | --------------------------------- | ---------------------------------- |
| US-001       | Create workspace                  | Workspace Switcher (inline create) |
| US-002       | List workspaces                   | Workspace Switcher                 |
| US-003       | Update workspace                  | Workspace Settings (General tab)   |
| US-004       | Add members                       | Member Management                  |
| US-005       | Delete workspace                  | Workspace Settings (Danger Zone)   |
| US-006       | Update member roles               | Member Management                  |
| US-008       | Fast workspace switching          | Workspace Switcher                 |
| US-009       | Share resources across workspaces | Resource Sharing UI                |
| US-010       | Configure workspace settings      | Workspace Settings                 |
| US-011       | List workspace members            | Member Management                  |
| US-012       | Remove members                    | Member Management                  |
| US-015       | View workspace details            | Workspace Settings (General tab)   |

### 1.3 Platform & Viewport Targets

| Target        | Viewport | Priority  | Notes                                   |
| ------------- | -------- | --------- | --------------------------------------- |
| Desktop       | 1440px   | Primary   | Full layout, all features               |
| Small desktop | 1024px   | Primary   | Tabs may scroll, sidebar collapses      |
| Tablet        | 768px    | Secondary | Single-column layout, hamburger nav     |
| Mobile        | 375px    | Noted     | Not wireframed; responsive via Tailwind |

---

## 2. Screen 1: Workspace Switcher

### 2.1 Context

The WorkspaceSwitcher is rendered in the app shell sidebar/header. It allows
users to switch between workspaces and create new ones. Per US-002 and US-008.

**Current state**: ✅ Implemented in `apps/web/src/components/WorkspaceSwitcher.tsx` (248 lines).

### 2.2 Wireframe — Desktop (1440px)

```
+--------------------------------------------------+
| WORKSPACE SWITCHER (Dropdown Trigger)             |
+--------------------------------------------------+
|                                                   |
|  [Icon: cube] Design Team          [ChevronDown] |
|               ADMIN                               |
|                                                   |
+--------------------------------------------------+

   ▼ Dropdown open (w-80, max-h-300px scroll)

+--------------------------------------------------+
| Workspaces                                       |
| 5 workspaces available                           |
+--------------------------------------------------+
| [Search input___________________________] 🔍     |
+--------------------------------------------------+
| ┌──────────────────────────────────────────────┐ |
| │ Design Team                        [✓]       │ |
| │ A collaborative design workspace             │ |
| │ [ADMIN]  3 teams                             │ |
| ├──────────────────────────────────────────────┤ |
| │ Engineering                                  │ |
| │ Core platform development                    │ |
| │ [MEMBER]  5 teams                            │ |
| ├──────────────────────────────────────────────┤ |
| │ Marketing                                    │ |
| │ Campaign management workspace                │ |
| │ [VIEWER]  2 teams                            │ |
| ├──────────────────────────────────────────────┤ |
| │ QA Testing                                   │ |
| │                                              │ |
| │ [MEMBER]  1 team                             │ |
| ├──────────────────────────────────────────────┤ |
| │ Executive                                    │ |
| │ Leadership workspace                         │ |
| │ [VIEWER]  0 teams                            │ |
| └──────────────────────────────────────────────┘ |
+--------------------------------------------------+
| [+ Plus icon] Create New Workspace               |
+--------------------------------------------------+

   ▼ Create form expanded (inline)

+--------------------------------------------------+
| [Input: Workspace name_____________________]     |
| [Error: "Workspace name is required"]            |
| [Create (primary)]  [Cancel (outline)]           |
+--------------------------------------------------+
```

### 2.3 Wireframe — Tablet (768px)

Identical layout. The switcher is rendered inside the sidebar overlay (per
Spec 005 `SidebarNav` behavior). Dropdown width: `w-full` within sidebar.

### 2.4 Interactive Elements

| Element            | Type               | Existing Component     | Notes                                                         |
| ------------------ | ------------------ | ---------------------- | ------------------------------------------------------------- |
| Trigger button     | `Button ghost`     | `Button` variant ghost | Min-w 200px, max-w 300px. Shows workspace name + role badge   |
| Dropdown container | `DropdownMenu`     | `DropdownMenu`         | w-80 (320px), max-h-300px with overflow-y-auto                |
| Search input       | `Input`            | `Input`                | **GAP**: Not yet implemented. Spec requires for >5 workspaces |
| Workspace row      | `DropdownMenuItem` | `DropdownMenuItem`     | Shows name, description (truncated), role badge, team count   |
| Role badge         | `Badge`            | `Badge`                | ADMIN=default, MEMBER=secondary, VIEWER=outline               |
| Check icon         | `Check`            | Lucide `Check`         | Shown on currently selected workspace                         |
| Create button      | `DropdownMenuItem` | `DropdownMenuItem`     | Primary text color, Plus icon, conditionally shown            |
| Create form        | Inline form        | `Input` + `Button`     | Name input, auto-generates slug, Create/Cancel buttons        |

### 2.5 States

| State            | Trigger                      | Visual Behavior                                                                 |
| ---------------- | ---------------------------- | ------------------------------------------------------------------------------- |
| Loading          | Initial mount, no data yet   | Spinner (border-animate) + "Loading workspaces..." text                         |
| Empty            | User has 0 workspaces        | Cube icon + "No workspaces" text. No dropdown. Create button shown if permitted |
| Default (closed) | Normal state                 | Trigger shows current workspace name, role, chevron                             |
| Open             | Click trigger                | Dropdown animates in. Workspace list scrollable if >5 items                     |
| Searching        | User types in search input   | List filters in real-time. "No results" message if 0 matches                    |
| Creating         | Click "Create New Workspace" | Inline form replaces the create button. Name input autofocuses                  |
| Create error     | Validation or API error      | Red error text below input: "Workspace name is required" or API message         |
| Create success   | API returns new workspace    | Form closes, new workspace added to list, auto-selected                         |
| Switching        | Click a different workspace  | Check icon moves to new selection, dropdown closes, context updates             |

### 2.6 Design Improvements Identified

Per the user journey analysis, the following improvements are recommended for
the existing implementation:

| #   | Improvement                                  | FR Reference | Priority |
| --- | -------------------------------------------- | ------------ | -------- |
| 1   | Add search/filter input when workspaces > 5  | US-002, 13.1 | HIGH     |
| 2   | Add keyboard shortcut hint (Cmd+K or Ctrl+K) | US-008       | MEDIUM   |
| 3   | Show "Shared resources" count per workspace  | US-009       | LOW      |

### 2.7 Accessibility (WCAG 2.1 AA)

| Criterion | Requirement                                                                                               | Status                                                             |
| --------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| 1.4.3     | Role badge text contrast ≥ 4.5:1 on badge background                                                      | ✅ Pass                                                            |
| 2.1.1     | Full keyboard operation: Tab → trigger, Enter → open, Arrow keys → navigate                               | 🟡 Partial — DropdownMenu provides, but search input not yet wired |
| 2.4.3     | Tab order: [1] Trigger → [2] Search → [3] Workspace items → [4] Create                                    | 🟡 Partial — search missing                                        |
| 2.4.7     | Focus ring visible on trigger and each menu item                                                          | ✅ Pass                                                            |
| 3.3.1     | Create form validation errors displayed as text below input                                               | ✅ Pass                                                            |
| 3.3.2     | Search input has placeholder "Search workspaces". Create input has placeholder "Workspace name"           | 🟡 Partial — needs visible labels                                  |
| 4.1.2     | Trigger: `aria-label="Switch workspace, current: {name}"`. List: `role="listbox"`, items: `aria-selected` | 🟡 Partial — spec 13.1 requirements not fully implemented          |

**Screen reader announcements**:

- On open: "Workspace selector, {N} workspaces available"
- On selection: "Switched to {workspace name}, role {role}"
- On create success: "Workspace {name} created and selected"

---

## 3. Screen 2: Workspace Settings

### 3.1 Context

The Settings page is a tabbed interface at `/settings`. It manages workspace
configuration. Per US-003, US-005, US-010, US-015.

**Current state**: 🟡 Partial. Page exists (790 lines) with General, Members,
Teams, Security, Billing, Integrations, Advanced, and Branding tabs. General
tab has name/description editing and placeholder toggle settings. **Gap 4
requires a typed settings form** replacing the hardcoded ToggleSetting
components with the 5-field schema from Appendix D.

### 3.2 Wireframe — Settings Page Shell (1440px)

```
+----------------------------------------------------------------------+
| [Sidebar]  Workspace Settings                                        |
|            Manage Design Team workspace settings and configuration    |
+----------------------------------------------------------------------+
|                                                                      |
| [General] [Members] [Teams] [Security] [Billing] [Integrations]     |
| [Advanced] [Branding*]                                               |
|                                                                      |
| ┌──────────────────────────── Tab Content ────────────────────────┐  |
| │                                                                 │  |
| │  (varies per tab — see wireframes below)                        │  |
| │                                                                 │  |
| └─────────────────────────────────────────────────────────────────┘  |
|                                                                      |
+----------------------------------------------------------------------+

* Branding tab visible only when ENABLE_TENANT_BRANDING flag is on
```

### 3.3 Wireframe — General Tab (Gap 4: Typed Settings Form)

```
+----------------------------------------------------------------------+
| GENERAL TAB                                                          |
+----------------------------------------------------------------------+
|                                                                      |
| ┌─────────── Workspace Information ───────────────────────────────┐  |
| │                                                                 │  |
| │  Workspace Name                                                 │  |
| │  [Design Team________________________________] (editable if     │  |
| │                                                  ADMIN+edit)    │  |
| │  Description                                                    │  |
| │  [A collaborative design workspace___________]                  │  |
| │  [____________________________________________]                  │  |
| │                                                                 │  |
| │  Slug           Created          Your Role                      │  |
| │  design-team    Jan 15, 2026     [ADMIN]                        │  |
| │  (monospace)                                                    │  |
| │                                                                 │  |
| │  [Edit Workspace]  (or  [Save Changes] [Cancel]  if editing)   │  |
| └─────────────────────────────────────────────────────────────────┘  |
|                                                                      |
| ┌─────────── Workspace Settings ──────────────────────────────────┐  |
| │                                                                 │  |
| │  Default Team Member Role                 [ADMIN ▼]             │  |
| │  Role assigned to new team members          (Select: ADMIN |   │  |
| │                                              MEMBER)            │  |
| │  ─────────────────────────────────────────────────────────────  │  |
| │  Allow Cross-Workspace Sharing            [  ○  OFF]            │  |
| │  Enable cross-workspace resource sharing                        │  |
| │  Hint: "Allows other workspace admins to share plugins          │  |
| │         with this workspace"                                    │  |
| │  ─────────────────────────────────────────────────────────────  │  |
| │  Maximum Members                          [0_________]          │  |
| │  Maximum workspace members (0 = unlimited)                      │  |
| │  ─────────────────────────────────────────────────────────────  │  |
| │  Workspace Discoverable                   [  ● ON ]             │  |
| │  Make workspace visible in tenant directory                     │  |
| │  ─────────────────────────────────────────────────────────────  │  |
| │                                                                 │  |
| │  [Unsaved changes indicator]                                    │  |
| │  [Save Settings (primary)]  [Discard (outline)]                 │  |
| └─────────────────────────────────────────────────────────────────┘  |
|                                                                      |
| ┌─────────── Danger Zone (ADMIN only) ────────────────────────────┐  |
| │  ⚠ border-destructive/50                                        │  |
| │                                                                 │  |
| │  Danger Zone                                                    │  |
| │  Deleting a workspace is permanent and cannot be undone.        │  |
| │  All teams and data will be lost.                               │  |
| │                                                                 │  |
| │  [Delete Workspace (destructive)]                               │  |
| │                                                                 │  |
| │  ── After clicking Delete: ──                                   │  |
| │  Are you absolutely sure? This action cannot be undone.         │  |
| │  [Yes, Delete Workspace (destructive)]  [Cancel (outline)]     │  |
| └─────────────────────────────────────────────────────────────────┘  |
|                                                                      |
+----------------------------------------------------------------------+
```

### 3.4 Wireframe — Settings Form Validation Error

```
+----------------------------------------------------------------------+
| ┌─────────── Workspace Settings ──────────────────────────────────┐  |
| │                                                                 │  |
| │  Maximum Members                          [-5________]          │  |
| │  ┌────────────────────────────────────────────────────┐        │  |
| │  │ ⚠ AlertCircle  Value must be 0 or greater         │        │  |
| │  └────────────────────────────────────────────────────┘        │  |
| │                                                                 │  |
| │  [Save Settings (primary, disabled)]  [Discard (outline)]      │  |
| └─────────────────────────────────────────────────────────────────┘  |
```

### 3.5 Wireframe — Tablet (768px)

At 768px:

- Tab list wraps or scrolls horizontally (single row with `overflow-x-auto`)
- Card sections stack full-width
- Settings form fields remain full-width within card
- Danger Zone card retains red border prominence

### 3.6 Interactive Elements

| Element                        | Type     | Component            | Notes                                                             |
| ------------------------------ | -------- | -------------------- | ----------------------------------------------------------------- |
| Tab navigation                 | Tabs     | `Tabs`/`TabsList`    | 7-8 tabs depending on feature flag. Scrollable at small viewports |
| Workspace name input           | Text     | `Input`              | Editable only for ADMINs in edit mode. Max 100 chars              |
| Description textarea           | Textarea | `Textarea`           | Editable only for ADMINs in edit mode. Max 500 chars              |
| Default Team Role select       | Select   | `Select`             | **NEW (Gap 4)**: Options: ADMIN, MEMBER. Default: MEMBER          |
| Cross-Workspace Sharing toggle | Toggle   | `Switch`             | **NEW (Gap 4)**: Default: OFF. Shows inline hint text             |
| Maximum Members input          | Number   | `Input type=number`  | **NEW (Gap 4)**: Min 0, max 10000. Default: 0 (unlimited)         |
| Workspace Discoverable toggle  | Toggle   | `Switch`             | **NEW (Gap 4)**: Default: ON                                      |
| Save Settings button           | Button   | `Button primary`     | **NEW (Gap 4)**: Disabled when no changes or validation errors    |
| Discard button                 | Button   | `Button outline`     | **NEW (Gap 4)**: Resets form to last saved state                  |
| Unsaved changes indicator      | Text     | Inline text          | **NEW (Gap 4)**: "You have unsaved changes" with dot indicator    |
| Edit Workspace button          | Button   | `Button primary`     | Toggles name/description to editable state. ADMIN-only            |
| Delete Workspace button        | Button   | `Button destructive` | Two-step confirmation. ADMIN-only                                 |

### 3.7 States

| State            | Trigger                        | Visual Behavior                                                         |
| ---------------- | ------------------------------ | ----------------------------------------------------------------------- |
| Loading          | Page mount                     | Skeleton placeholders for all sections                                  |
| No workspace     | No currentWorkspace in context | Empty state: cube icon, "No Workspace Selected", prompt to use switcher |
| View mode        | Default for ADMIN              | Name/description shown as text. Settings form interactive               |
| Edit mode        | ADMIN clicks "Edit Workspace"  | Name/description become editable inputs. Save/Cancel buttons appear     |
| Read-only        | User role is MEMBER or VIEWER  | All settings shown with values but no edit controls. No Save button     |
| Unsaved changes  | Any setting value differs      | Yellow dot + "Unsaved changes" text. Save button enabled                |
| Saving           | Click Save Settings            | Button shows spinner + "Saving...". Inputs disabled                     |
| Save success     | API returns 200                | Success toast: "Workspace settings saved". Unsaved indicator clears     |
| Save error       | API returns 4xx/5xx            | Error toast with API message. Form remains in unsaved state             |
| Validation error | Invalid field value            | Inline error below field (Alert variant=destructive). Save disabled     |
| Delete confirm   | Click Delete Workspace         | Inline confirmation replaces button. Two buttons: confirm + cancel      |

### 3.8 Accessibility (WCAG 2.1 AA)

| Criterion | Requirement                                                                                                            | Status                |
| --------- | ---------------------------------------------------------------------------------------------------------------------- | --------------------- |
| 1.4.3     | All form label text ≥ 4.5:1 contrast. Error text (destructive) ≥ 4.5:1                                                 | ✅ Pass               |
| 1.4.4     | Form resizable to 200% without overlap. Single-column at narrow viewports                                              | ✅ Pass               |
| 2.1.1     | All tabs, inputs, toggles, buttons keyboard-accessible                                                                 | ✅ Pass               |
| 2.4.3     | Tab order: [1] Tab list → [2] Form fields top-to-bottom → [3] Save/Cancel → [4] Danger Zone                            | ✅ Pass               |
| 2.4.7     | Focus ring on all interactive elements (`--ring` token)                                                                | ✅ Pass               |
| 3.3.1     | Validation errors: text below field, red border on input, Alert with icon                                              | ✅ Pass               |
| 3.3.2     | All inputs have visible `Label` components above them. Switch has text label to the left                               | ✅ Pass               |
| 4.1.2     | Tabs: `role="tablist"`, `aria-selected`. Switch: `role="switch"`, `aria-checked`. Delete confirm: `aria-live="polite"` | 🟡 Needs verification |

**Screen reader announcements**:

- On tab switch: "General tab selected, 1 of 7"
- On toggle change: "{setting name} is now {on|off}"
- On save success: "Settings saved successfully"
- On validation error: "{field name}: {error message}"
- On delete confirm: "Warning: workspace deletion is permanent. Press confirm to delete or cancel to go back"

---

## 4. Screen 3: Resource Sharing UI

### 4.1 Context

The Resource Sharing UI enables workspace admins to share plugins across
workspaces within the same tenant. Per US-009, Gap 3.

**Current state**: 🔴 Not implemented. Three new API endpoints exist in spec
but have no frontend. This is a **net-new UI**.

**Precondition**: `allowCrossWorkspaceSharing` must be `true` in workspace
settings (Gap 4 must be implemented first).

### 4.2 Wireframe — Share Resources Tab (New Tab on Settings Page)

The Resource Sharing UI is added as a new **"Sharing"** tab on the Workspace
Settings page, positioned after "Teams" and before "Security":

```
+----------------------------------------------------------------------+
| [General] [Members] [Teams] [Sharing] [Security] [Billing] ...      |
+----------------------------------------------------------------------+
|                                                                      |
| ┌─────────── Shared Resources ────────────────────────────────────┐  |
| │                                                                 │  |
| │  Shared Resources (3)                    [Share Plugin (primary)]│  |
| │                                                                 │  |
| │  ┌────────────────────────────────────────────────────────────┐ │  |
| │  │ [Puzzle] Analytics Pro                                     │ │  |
| │  │          Shared with: Engineering, QA Testing              │ │  |
| │  │          Shared by: maria@acme.com on Feb 20, 2026         │ │  |
| │  │          [Badge: PLUGIN]              [Revoke (ghost)]     │ │  |
| │  ├────────────────────────────────────────────────────────────┤ │  |
| │  │ [Puzzle] Design Toolkit                                    │ │  |
| │  │          Shared with: Marketing                            │ │  |
| │  │          Shared by: maria@acme.com on Feb 22, 2026         │ │  |
| │  │          [Badge: PLUGIN]              [Revoke (ghost)]     │ │  |
| │  ├────────────────────────────────────────────────────────────┤ │  |
| │  │ [Puzzle] Reporting Engine                                  │ │  |
| │  │          Shared with: Executive                            │ │  |
| │  │          Shared by: john@acme.com on Feb 25, 2026          │ │  |
| │  │          [Badge: PLUGIN]              [Revoke (ghost)]     │ │  |
| │  └────────────────────────────────────────────────────────────┘ │  |
| │                                                                 │  |
| └─────────────────────────────────────────────────────────────────┘  |
|                                                                      |
| ┌─────────── Resources Shared With Me ────────────────────────────┐  |
| │                                                                 │  |
| │  Resources Shared With This Workspace (1)                       │  |
| │                                                                 │  |
| │  ┌────────────────────────────────────────────────────────────┐ │  |
| │  │ [Puzzle] CI Pipeline Runner                                │ │  |
| │  │          Shared from: Engineering                          │ │  |
| │  │          Shared by: kenji@acme.com on Feb 18, 2026         │ │  |
| │  │          [Badge: PLUGIN]                                   │ │  |
| │  └────────────────────────────────────────────────────────────┘ │  |
| │                                                                 │  |
| └─────────────────────────────────────────────────────────────────┘  |
|                                                                      |
+----------------------------------------------------------------------+
```

### 4.3 Wireframe — Share Plugin Dialog

```
+----------------------------------------------------------------------+
| ┌──────────────── Share Plugin ───────────────────────────────────┐  |
| │                                                                 │  |
| │  Share a plugin with other workspaces in your tenant.           │  |
| │                                                                 │  |
| │  Select Plugin                                                  │  |
| │  [Search installed plugins...______________________] 🔍         │  |
| │  ┌──────────────────────────────────────────────────────────┐  │  |
| │  │ [✓] Analytics Pro                                        │  │  |
| │  │ [ ] CI Pipeline Runner                                   │  │  |
| │  │ [ ] Design Toolkit                                       │  │  |
| │  │ [ ] Reporting Engine                                     │  │  |
| │  └──────────────────────────────────────────────────────────┘  │  |
| │                                                                 │  |
| │  Select Target Workspace(s)                                     │  |
| │  [Search workspaces...________________________] 🔍              │  |
| │  ┌──────────────────────────────────────────────────────────┐  │  |
| │  │ [✓] Engineering                                          │  │  |
| │  │ [✓] QA Testing                                           │  │  |
| │  │ [ ] Marketing                                            │  │  |
| │  │ [ ] Executive                                            │  │  |
| │  │ [─] Design Team  (current workspace, disabled)           │  │  |
| │  └──────────────────────────────────────────────────────────┘  │  |
| │                                                                 │  |
| │  ⓘ Selected: Analytics Pro → Engineering, QA Testing           │  |
| │                                                                 │  |
| │                        [Cancel (outline)]  [Share (primary)]    │  |
| │                                                                 │  |
| └─────────────────────────────────────────────────────────────────┘  |
```

### 4.4 Wireframe — Sharing Disabled State

```
+----------------------------------------------------------------------+
| ┌─────────── Sharing ─────────────────────────────────────────────┐  |
| │                                                                 │  |
| │  ┌────────────────────────────────────────────────────────────┐ │  |
| │  │ [Lock icon]                                                │ │  |
| │  │                                                            │ │  |
| │  │ Cross-Workspace Sharing is Disabled                        │ │  |
| │  │                                                            │ │  |
| │  │ Resource sharing must be enabled in workspace settings     │ │  |
| │  │ before you can share plugins with other workspaces.        │ │  |
| │  │                                                            │ │  |
| │  │ [Go to Settings (primary)]                                 │ │  |
| │  └────────────────────────────────────────────────────────────┘ │  |
| │                                                                 │  |
| └─────────────────────────────────────────────────────────────────┘  |
```

### 4.5 Wireframe — Empty State (No Shared Resources)

```
+----------------------------------------------------------------------+
| ┌─────────── Shared Resources ────────────────────────────────────┐  |
| │                                                                 │  |
| │  ┌────────────────────────────────────────────────────────────┐ │  |
| │  │ [Share2 icon]                                              │ │  |
| │  │                                                            │ │  |
| │  │ No resources shared yet                                    │ │  |
| │  │                                                            │ │  |
| │  │ Share plugins with other workspaces to enable              │ │  |
| │  │ collaboration across teams.                                │ │  |
| │  │                                                            │ │  |
| │  │ [Share Your First Plugin (primary)]                        │ │  |
| │  └────────────────────────────────────────────────────────────┘ │  |
| │                                                                 │  |
| └─────────────────────────────────────────────────────────────────┘  |
```

### 4.6 Wireframe — Revoke Confirmation

```
+----------------------------------------------------------------------+
| ┌──────────────── Revoke Sharing ─────────────────────────────────┐  |
| │                                                                 │  |
| │  Are you sure you want to stop sharing "Analytics Pro"          │  |
| │  with Engineering?                                              │  |
| │                                                                 │  |
| │  The Engineering workspace will lose access to this plugin.     │  |
| │                                                                 │  |
| │                    [Cancel (outline)]  [Revoke (destructive)]   │  |
| │                                                                 │  |
| └─────────────────────────────────────────────────────────────────┘  |
```

### 4.7 Interactive Elements

| Element                      | Type          | Component          | Notes                                                     |
| ---------------------------- | ------------- | ------------------ | --------------------------------------------------------- |
| Share Plugin button          | Button        | `Button primary`   | ADMIN-only. Opens Share Plugin dialog                     |
| Shared resource row          | List item     | Custom (Card-like) | Plugin icon, name, shared-with list, sharer, date, revoke |
| Resource type badge          | Badge         | `Badge secondary`  | Shows "PLUGIN" (only type currently supported)            |
| Revoke button                | Button        | `Button ghost`     | ADMIN-only. Opens revoke confirmation dialog              |
| Share dialog                 | Dialog        | `Dialog`           | Two-section: plugin selector + workspace selector         |
| Plugin selector              | Checkbox list | `Checkbox` + list  | Searchable list of installed plugins                      |
| Workspace selector           | Checkbox list | `Checkbox` + list  | Searchable list of tenant workspaces. Current WS disabled |
| Selection summary            | Info text     | Inline text        | Shows "Selected: {plugin} → {workspace1}, {workspace2}"   |
| Share confirm button         | Button        | `Button primary`   | Disabled until ≥1 plugin and ≥1 target workspace selected |
| Revoke confirm dialog        | Dialog        | `Dialog`           | Names the specific plugin + target workspace. Destructive |
| Sharing disabled empty state | Empty state   | Custom             | Lock icon, explanation text, CTA to Settings              |
| No resources empty state     | Empty state   | `EmptyState`       | Share icon, explanation text, CTA to share first plugin   |

### 4.8 States

| State             | Trigger                                | Visual Behavior                                                 |
| ----------------- | -------------------------------------- | --------------------------------------------------------------- |
| Loading           | Tab mount                              | Skeleton rows (3 lines) for shared resources list               |
| Sharing disabled  | `allowCrossWorkspaceSharing === false` | Full-tab empty state with lock icon, explanation, settings link |
| Empty (outbound)  | No resources shared from this WS       | Empty state with share icon + CTA button                        |
| Empty (inbound)   | No resources shared to this WS         | "No resources shared with this workspace" text                  |
| Default           | Resources exist                        | Two sections: "Shared Resources" and "Shared With Me"           |
| Dialog open       | Click "Share Plugin"                   | Modal dialog with two searchable checkbox lists                 |
| Dialog searching  | Type in plugin/workspace search        | Lists filter in real-time                                       |
| Dialog ready      | ≥1 plugin + ≥1 workspace selected      | Summary text shows selection. Share button enabled              |
| Sharing           | Click Share                            | Button shows spinner + "Sharing...". Inputs disabled            |
| Share success     | API returns 201                        | Dialog closes. Success toast. Resource list refreshes           |
| Share conflict    | API returns 409                        | Error toast: "Analytics Pro is already shared with Engineering" |
| Revoke confirm    | Click Revoke on a resource             | Confirmation dialog with resource name + target workspace       |
| Revoking          | Click Revoke in confirm dialog         | Button shows spinner + "Revoking..."                            |
| Revoke success    | API returns 204                        | Dialog closes. Success toast. Resource removed from list        |
| Permission denied | Non-ADMIN views tab                    | Read-only: sees shared resources but no Share/Revoke buttons    |
| API error         | Any 5xx response                       | Error toast with message. UI remains in previous state          |

### 4.9 Accessibility (WCAG 2.1 AA)

| Criterion | Requirement                                                                                                              | Status       |
| --------- | ------------------------------------------------------------------------------------------------------------------------ | ------------ |
| 1.4.3     | All text meets 4.5:1 contrast. Badge text on badge bg meets ratio                                                        | ✅ By design |
| 1.4.4     | Dialog responsive: scrollable at 200% zoom. Lists don't overflow                                                         | ✅ By design |
| 2.1.1     | Dialog keyboard-navigable: Tab cycles focus within dialog (focus trap)                                                   | ✅ Dialog    |
| 2.4.3     | Dialog tab order: [1] Plugin search → [2] Plugin checkboxes → [3] WS search → [4] WS checkboxes → [5] Cancel → [6] Share | ✅ By design |
| 2.4.7     | Focus ring on all checkboxes, search inputs, and buttons                                                                 | ✅ By design |
| 3.3.1     | Conflict error: toast with specific message naming the resource + workspace                                              | ✅ By design |
| 3.3.2     | Search inputs: visible label above ("Select Plugin", "Select Target Workspace(s)")                                       | ✅ By design |
| 4.1.2     | Checkboxes: `aria-label="{plugin/workspace name}"`. Disabled current WS: `aria-disabled="true"`                          | ✅ By design |

**Screen reader announcements**:

- On dialog open: "Share Plugin dialog. Select a plugin and target workspaces."
- On checkbox toggle: "{name} selected" / "{name} deselected"
- On share success: "Plugin {name} shared with {workspace1} and {workspace2}"
- On revoke: "Sharing of {plugin} with {workspace} has been revoked"
- On sharing disabled: "Cross-workspace sharing is disabled. Navigate to Settings to enable it."

---

## 5. Screen 4: Member Management

### 5.1 Context

The Member Management UI is the "Members" tab on the Settings page. It
displays the workspace member list with role editing and add/remove
capabilities. Per US-004, US-006, US-011, US-012.

**Current state**: ✅ Implemented in `settings.tsx` (MembersTab component,
lines 398-542). Add Member dialog implemented (lines 558-643).

### 5.2 Wireframe — Members Tab (1440px)

```
+----------------------------------------------------------------------+
| MEMBERS TAB                                                          |
+----------------------------------------------------------------------+
|                                                                      |
| ┌─────────── Members (12) ────────────────────────────────────────┐  |
| │                                                     [Add Member]│  |
| │                                                                 │  |
| │  ┌────────────────────────────────────────────────────────────┐ │  |
| │  │ [Avatar: M]  Maria Rodriguez                               │ │  |
| │  │              maria@acme.com          [ADMIN ▼]    [Remove] │ │  |
| │  ├────────────────────────────────────────────────────────────┤ │  |
| │  │ [Avatar: K]  Kenji Tanaka            (You)                 │ │  |
| │  │              kenji@acme.com          [ADMIN]               │ │  |
| │  ├────────────────────────────────────────────────────────────┤ │  |
| │  │ [Avatar: P]  Priya Sharma                                  │ │  |
| │  │              priya@acme.com          [VIEWER ▼]   [Remove] │ │  |
| │  ├────────────────────────────────────────────────────────────┤ │  |
| │  │ [Avatar: J]  John Chen                                     │ │  |
| │  │              john@acme.com           [MEMBER ▼]   [Remove] │ │  |
| │  ├────────────────────────────────────────────────────────────┤ │  |
| │  │               ... (scrollable list)                        │ │  |
| │  └────────────────────────────────────────────────────────────┘ │  |
| │                                                                 │  |
| │  Showing 12 of 12 members                                      │  |
| │                                                                 │  |
| └─────────────────────────────────────────────────────────────────┘  |
|                                                                      |
+----------------------------------------------------------------------+

Legend:
  [ADMIN ▼]  = Role dropdown (editable by ADMIN, not for self)
  [ADMIN]    = Role badge (non-editable, for current user)
  [Remove]   = Destructive text link (ADMIN only, not for self)
  (You)      = Indicator for current user's row
```

### 5.3 Wireframe — Add Member Dialog

```
+----------------------------------------------------------------------+
| ┌──────────────── Add Member ─────────────────────────────────────┐  |
| │                                                                 │  |
| │  Invite a user to this workspace by email address               │  |
| │                                                                 │  |
| │  Email Address                                                  │  |
| │  [member@example.com_______________________________]            │  |
| │                                                                 │  |
| │  Role                                                           │  |
| │  [MEMBER ▼]                                                     │  |
| │   ├─ Member                                                     │  |
| │   ├─ Admin                                                      │  |
| │   └─ Viewer                                                     │  |
| │                                                                 │  |
| │                        [Cancel (outline)]  [Add Member (primary)]│  |
| │                                                                 │  |
| └─────────────────────────────────────────────────────────────────┘  |
```

### 5.4 Wireframe — Empty Members State

```
+----------------------------------------------------------------------+
| ┌─────────── Members (0) ─────────────────────────────────────────┐  |
| │                                                    [Add Member] │  |
| │                                                                 │  |
| │            No members found                                     │  |
| │                                                                 │  |
| └─────────────────────────────────────────────────────────────────┘  |
```

### 5.5 Interactive Elements

| Element              | Type        | Component          | Notes                                                                |
| -------------------- | ----------- | ------------------ | -------------------------------------------------------------------- |
| Add Member button    | Button      | `Button primary`   | ADMIN-only. Opens Add Member dialog                                  |
| Member row           | List item   | Custom div         | Avatar, name, email, role control, remove button                     |
| Avatar               | Avatar      | Inline div         | First letter of firstName (or email). Primary bg, white text         |
| "(You)" indicator    | Text        | Inline span        | Muted text showing which row is the current user                     |
| Role dropdown        | Select      | Native `<select>`  | ADMIN-only, not shown for current user. Options: ADMIN/MEMBER/VIEWER |
| Role badge           | Badge       | `Badge`            | Shown instead of dropdown for current user or non-ADMIN viewers      |
| Remove button        | Text button | `<button>`         | ADMIN-only, not for current user. Destructive color                  |
| Add Member dialog    | Dialog      | `Dialog`           | Email input + role select + Cancel/Add buttons                       |
| Email input          | Email input | `Input type=email` | With `Label` and validation error via `Alert`                        |
| Role select (dialog) | Select      | Native `<select>`  | Default: MEMBER. Options: MEMBER, ADMIN, VIEWER                      |

### 5.6 States

| State           | Trigger                  | Visual Behavior                                                     |
| --------------- | ------------------------ | ------------------------------------------------------------------- |
| Loading         | Tab mount / data fetch   | Centered spinner with border animation                              |
| Error           | API error on fetch       | Red error banner with message text                                  |
| Empty           | 0 members returned       | "No members found" centered text. Add Member button still visible   |
| Default         | Members loaded           | Scrollable member list with role controls                           |
| Role changing   | Change role dropdown     | Optimistic update; success toast "Member role updated"              |
| Role error      | API error on role update | Error toast with message. Role reverts to previous value            |
| Remove confirm  | Click Remove             | `confirm()` dialog **[ANTI-PATTERN — should use Dialog component]** |
| Removing        | Confirm removal          | Row removed optimistically. Success toast "Member removed"          |
| Remove error    | API error on removal     | Error toast. Row re-appears                                         |
| Adding (dialog) | Dialog open              | Email + role form. Add Member button spinner on submit              |
| Add success     | API returns 200          | Dialog closes. Success toast. Member list refreshes                 |
| Add error       | API error (409, 404)     | Error toast with specific message                                   |

### 5.7 Design Improvements Identified

| #   | Improvement                                          | FR Reference                         | Priority |
| --- | ---------------------------------------------------- | ------------------------------------ | -------- |
| 1   | Replace `confirm()` with `Dialog` for member removal | Anti-pattern in design-system.md §11 | HIGH     |
| 2   | Add search/filter for large member lists (>20)       | US-011                               | MEDIUM   |
| 3   | Add pagination for workspaces with 50+ members       | US-011                               | MEDIUM   |
| 4   | Replace native `<select>` with `Select` component    | Design consistency                   | LOW      |
| 5   | Add user search (typeahead) in Add Member dialog     | US-004                               | MEDIUM   |

### 5.8 Accessibility (WCAG 2.1 AA)

| Criterion | Requirement                                                                                                             | Status                            |
| --------- | ----------------------------------------------------------------------------------------------------------------------- | --------------------------------- |
| 1.4.3     | Avatar initial text (white on primary) contrast ≥ 4.5:1                                                                 | ✅ Pass (5.3:1 light, 4.6:1 dark) |
| 1.4.4     | Member list wraps cleanly at 200% zoom. Single column at narrow viewports                                               | ✅ Pass                           |
| 2.1.1     | All interactive elements (role dropdown, remove button, dialog) keyboard-accessible                                     | ✅ Pass                           |
| 2.4.3     | Tab order within member row: [1] Role dropdown → [2] Remove button. Dialog: [1] Email → [2] Role → [3] Cancel → [4] Add | ✅ Pass                           |
| 2.4.7     | Focus ring on dropdown, remove button, dialog inputs                                                                    | ✅ Pass                           |
| 3.3.1     | Add Member: email validation error shown as Alert with icon + text                                                      | ✅ Pass                           |
| 3.3.2     | Dialog: visible Labels for "Email Address" and "Role"                                                                   | ✅ Pass                           |
| 4.1.2     | Role dropdown: needs `aria-label="Change role for {name}"`. Remove button: `aria-label="Remove {name} from workspace"`  | 🟡 Needs implementation           |

**Screen reader announcements**:

- On member list load: "Members list, {N} members"
- On role change: "{name}'s role changed to {role}"
- On member removed: "{name} removed from workspace"
- On member added: "{name} added as {role}"

---

## 6. Component Specifications

### 6.1 New Components Needed (Spec 009)

| Component                   | Purpose                                                      | Screens Used                     |
| --------------------------- | ------------------------------------------------------------ | -------------------------------- |
| `WorkspaceSettingsForm`     | Typed settings form (5 fields) per Appendix D schema         | Workspace Settings (General tab) |
| `SharePluginDialog`         | Multi-select dialog for sharing plugins across workspaces    | Resource Sharing tab             |
| `SharedResourceRow`         | List item displaying a shared resource with metadata/actions | Resource Sharing tab             |
| `RevokeShareDialog`         | Confirmation dialog for revoking a resource share            | Resource Sharing tab             |
| `SharingDisabledEmptyState` | Empty state when cross-workspace sharing is off              | Resource Sharing tab             |

### 6.2 Component: WorkspaceSettingsForm

**Purpose**: Renders the 5-field workspace settings form from the Zod schema
in Appendix D. Replaces the hardcoded `ToggleSetting` components.

**Props**:

| Prop       | Type                                    | Required | Default | Description                               |
| ---------- | --------------------------------------- | -------- | ------- | ----------------------------------------- |
| `settings` | `WorkspaceSettings`                     | Yes      | —       | Current settings values from API          |
| `onSave`   | `(settings: WorkspaceSettings) => void` | Yes      | —       | Callback when user saves valid settings   |
| `isAdmin`  | `boolean`                               | Yes      | —       | Controls editability (read-only if false) |
| `isSaving` | `boolean`                               | No       | `false` | Shows loading state on Save button        |

**Renders** (top to bottom):

1. **Default Team Role** — `Select` with options ADMIN, MEMBER
2. **Allow Cross-Workspace Sharing** — `Switch` with inline hint text
3. **Maximum Members** — `Input type=number` with min=0, max=10000 validation
4. **Workspace Discoverable** — `Switch`
5. **Unsaved indicator** — Text "You have unsaved changes" (hidden when clean)
6. **Save/Discard buttons** — `Button primary` + `Button outline`

**Validation**: Client-side via Zod `WorkspaceSettingsSchema.safeParse()`.
Inline errors displayed below each invalid field.

**Interaction states**:

| State     | Visual                                                         |
| --------- | -------------------------------------------------------------- |
| Clean     | All fields show current values. Save button hidden or disabled |
| Dirty     | Unsaved indicator visible. Save + Discard buttons visible      |
| Invalid   | Dirty + inline error(s). Save button disabled                  |
| Saving    | Save button: spinner + "Saving...". All fields disabled        |
| Read-only | All controls replaced with text values. No Save button         |

### 6.3 Component: SharePluginDialog

**Purpose**: Modal for selecting a plugin and target workspace(s) to share.

**Props**:

| Prop           | Type                      | Required | Default | Description                                   |
| -------------- | ------------------------- | -------- | ------- | --------------------------------------------- |
| `open`         | `boolean`                 | Yes      | —       | Controls dialog visibility                    |
| `onOpenChange` | `(open: boolean) => void` | Yes      | —       | Close callback                                |
| `workspaceId`  | `string`                  | Yes      | —       | Current workspace ID (excluded from targets)  |
| `onSuccess`    | `() => void`              | Yes      | —       | Callback on successful share (refreshes list) |

**Internal state**:

- `selectedPluginId: string | null`
- `selectedWorkspaceIds: string[]`
- `pluginSearch: string`
- `workspaceSearch: string`
- `isSharing: boolean`

**API calls**:

- On mount: `GET /api/workspaces/:id/plugins` (installed plugins)
- On mount: `GET /api/workspaces` (other workspaces in tenant)
- On submit: `POST /api/workspaces/:id/resources/share` for each target

### 6.4 Component: SharedResourceRow

**Purpose**: Renders a single shared resource in the resource list.

**Props**:

| Prop       | Type                           | Required | Default | Description                             |
| ---------- | ------------------------------ | -------- | ------- | --------------------------------------- |
| `resource` | `SharedResource`               | Yes      | —       | The shared resource object              |
| `onRevoke` | `(resourceId: string) => void` | No       | —       | Callback for revoke action (ADMIN-only) |
| `isAdmin`  | `boolean`                      | Yes      | —       | Controls visibility of Revoke button    |

**Renders**:

- Plugin icon (Puzzle from Lucide)
- Resource name (bold)
- "Shared with: {workspace list}" or "Shared from: {workspace}"
- "Shared by: {email} on {date}"
- Type badge (`Badge secondary`: "PLUGIN")
- Revoke button (`Button ghost`, ADMIN-only)

### 6.5 Component: RevokeShareDialog

**Purpose**: Confirmation dialog before revoking a shared resource.

**Props**:

| Prop            | Type                      | Required | Default | Description                          |
| --------------- | ------------------------- | -------- | ------- | ------------------------------------ |
| `open`          | `boolean`                 | Yes      | —       | Controls dialog visibility           |
| `onOpenChange`  | `(open: boolean) => void` | Yes      | —       | Close callback                       |
| `resourceName`  | `string`                  | Yes      | —       | Plugin name for confirmation message |
| `workspaceName` | `string`                  | Yes      | —       | Target workspace name for message    |
| `onConfirm`     | `() => Promise<void>`     | Yes      | —       | Revoke action callback               |

### 6.6 Component: SharingDisabledEmptyState

**Purpose**: Full-tab empty state shown when `allowCrossWorkspaceSharing` is
`false` in workspace settings.

**Props**:

| Prop      | Type      | Required | Default | Description                    |
| --------- | --------- | -------- | ------- | ------------------------------ |
| `isAdmin` | `boolean` | Yes      | —       | If true, shows CTA to settings |

**Renders**:

- Lock icon (Lucide `Lock`)
- "Cross-Workspace Sharing is Disabled" heading
- Explanation text
- "Go to Settings" button (if ADMIN) — navigates to General tab

---

## 7. Design Tokens (Spec 009)

### 7.1 New Tokens

No new color tokens are needed for Spec 009. All visual states map to
existing tokens from the design system:

| Visual Element             | Token Used             | Rationale                          |
| -------------------------- | ---------------------- | ---------------------------------- |
| Sharing badge ("PLUGIN")   | `Badge secondary`      | Non-critical metadata indicator    |
| Unsaved changes indicator  | `--status-warning`     | Attention without alarm            |
| Sharing disabled lock icon | `--muted-foreground`   | Neutral disabled state             |
| Revoke button              | `--destructive`        | Destructive action                 |
| Success toast (save/share) | `--status-success`     | Standard success feedback          |
| Settings card border       | `--border`             | Standard card border               |
| Danger zone card border    | `--destructive` at 50% | Existing pattern from current code |
| Avatar circle background   | `--primary`            | Existing pattern from current code |
| Avatar initial text        | `--primary-foreground` | White on primary                   |

### 7.2 New Spacing Patterns

| Pattern                     | Token       | Value | Usage                                            |
| --------------------------- | ----------- | ----- | ------------------------------------------------ |
| Settings form field spacing | `--space-4` | 16px  | Between consecutive form fields                  |
| Settings section spacing    | `--space-6` | 24px  | Between Workspace Information and Settings cards |
| Dialog internal padding     | `--space-5` | 20px  | Padding inside Share/Revoke dialogs              |
| Shared resource row padding | `--space-3` | 12px  | Vertical padding inside resource rows            |

### 7.3 New Icon Usage

| Icon   | Lucide Name | Usage                                         |
| ------ | ----------- | --------------------------------------------- |
| Share  | `Share2`    | Share Plugin button, empty state illustration |
| Lock   | `Lock`      | Sharing disabled state                        |
| Puzzle | `Puzzle`    | Plugin icon in shared resource rows           |
| Unlink | `Unlink`    | Revoke sharing action (alternative to Trash)  |

---

## 8. Responsive Behavior

### 8.1 Breakpoint Summary

| Breakpoint | Workspace Switcher             | Settings Page                       | Resource Sharing             | Member Management               |
| ---------- | ------------------------------ | ----------------------------------- | ---------------------------- | ------------------------------- |
| ≥1440px    | Sidebar trigger, w-80 dropdown | Full tab row, 2-col metadata        | Two card sections, full rows | Full member rows                |
| 1024px     | Same as 1440px                 | Tab row scrolls if needed           | Same as 1440px               | Same as 1440px                  |
| 768px      | Inside sidebar overlay         | Tabs scroll horizontally            | Cards full-width, stacked    | Single column, stacked controls |
| <768px     | Full-width in sidebar overlay  | Single tab visible, swipe to switch | Dialog nearly full-screen    | Avatar + name stack, role below |

### 8.2 Settings Page Tablet Behavior (768px)

- Tab list becomes horizontally scrollable with `overflow-x-auto`
- All card sections become full-width
- Form fields stack vertically (already single-column)
- Danger Zone section remains visually prominent (red border)

### 8.3 Share Dialog Tablet Behavior (768px)

- Dialog width: `sm:max-w-lg` → near full-width at 768px
- Plugin and workspace lists become taller (max-h increases)
- Two checkbox lists stack vertically instead of side-by-side

---

## 9. Cross-References

| Document                    | Path                                                    |
| --------------------------- | ------------------------------------------------------- |
| Feature spec                | `.forge/specs/009-workspace-management/spec.md`         |
| User journeys               | `.forge/specs/009-workspace-management/user-journey.md` |
| Design system (global)      | `.forge/ux/design-system.md`                            |
| Architecture                | `.forge/architecture/architecture.md`                   |
| Constitution (UX standards) | `.forge/constitution.md` Art. 1.3                       |
| WorkspaceSwitcher source    | `apps/web/src/components/WorkspaceSwitcher.tsx`         |
| WorkspaceContext source     | `apps/web/src/contexts/WorkspaceContext.tsx`            |
| Settings page source        | `apps/web/src/routes/settings.tsx`                      |

---

## 10. Open Design Questions

All questions resolved on 2026-03-02.

| #   | Question                                                                                                                   | Impact                   | Resolution                                                                                                                                                           | Status      |
| --- | -------------------------------------------------------------------------------------------------------------------------- | ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| 1   | Should the Share Plugin dialog support selecting multiple plugins at once?                                                 | Implementation           | **Yes — multi-select checkboxes.** Update `SharePluginDialog` wireframe and component spec to use checkbox list. Batch sharing reduces clicks for power-user admins. | ✅ Resolved |
| 2   | Should the Sharing tab show resources shared TO this workspace by default, or require a separate "Shared With Me" section? | Information architecture | **Both sections on the same Sharing tab** — "Shared Resources" (outbound) + "Resources Shared With Me" (inbound), as wireframed.                                     | ✅ Resolved |
| 3   | Should member removal use `Dialog` component instead of `confirm()`?                                                       | Quality/a11y             | **Yes — replace `confirm()` with `Dialog` component.** Required for WCAG 2.1 AA compliance and consistency with design-system.md §11.                                | ✅ Resolved |

---

## 13. Accessibility Audit & Resolution (T9 — WCAG 2.1 AA)

> **Status: ✅ All gaps resolved** — March 5, 2026 (T9.1–T9.5)

The UX design audit identified 4 WCAG 2.1 AA accessibility gaps across 3 existing
workspace screens. All gaps were addressed in Phase 9 (T9.1–T9.3) and verified by
automated axe-core Playwright tests (T9.4). See `tasks.md` Phase 9 for implementation
details.

### 13.1 Gap Resolution Summary

| #   | Screen            | Gap Description                                                 | Fix Applied                                                                          | Verified by | Status      |
| --- | ----------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ----------- | ----------- |
| 1   | WorkspaceSwitcher | Trigger button lacked `aria-label`                              | Added `aria-label="Switch workspace, current: {name}"` to trigger `Button`           | T9.1 / T9.4 | ✅ Resolved |
| 2   | WorkspaceSwitcher | Search input lacked visible label (placeholder only)            | Added `aria-label="Search workspaces"` on `Input` (hidden label pattern, WCAG 1.3.1) | T9.1 / T9.4 | ✅ Resolved |
| 3   | Settings          | Toggle controls missing `role="switch"` and `aria-checked`      | Added `role="switch"` and `aria-checked={value}` to all toggle controls              | T9.2 / T9.4 | ✅ Resolved |
| 4   | Members           | Role dropdown and remove button lacked descriptive `aria-label` | Added `aria-label="Change role for {name}"` and `aria-label="Remove {name}"`         | T9.3 / T9.4 | ✅ Resolved |

### 13.2 ARIA Pattern Note: WorkspaceSwitcher

The original gap specification (§2 ARIA analysis, line 168) described the fix as
adding `role="listbox"` / `aria-selected`. During implementation (T9.1), it was
determined that `role="listbox"` is semantically incorrect for a navigation-style
workspace switcher:

- **`role="listbox"`** is appropriate for form select controls (selecting a value to submit)
- **`role="menu"`** (rendered by Radix `DropdownMenu`) is appropriate for navigation/action
  menus — which matches the WorkspaceSwitcher's purpose (navigating between workspaces)
- **`aria-current="true"`** (used instead of `aria-selected`) correctly indicates the
  currently-active workspace in a navigation context (WCAG 4.1.2)

This implementation passes all axe-core WCAG 2.1 AA rules with 0 violations.
The trigger button (`aria-label`) and the full dropdown menu are both included in
the T9.4 scan scope (`[data-workspace-switcher]` container).

### 13.3 Test Coverage

All 3 screens plus both dialog states are covered by automated axe-core tests in
`apps/web/tests/e2e/workspace-a11y.spec.ts` (T9.4):

| Test Case                                 | Scope                       | WCAG Tags                 |
| ----------------------------------------- | --------------------------- | ------------------------- |
| WorkspaceSwitcher dropdown                | `[data-workspace-switcher]` | wcag2a, wcag2aa, wcag21aa |
| Workspace Settings page                   | Full page                   | wcag2a, wcag2aa, wcag21aa |
| Workspace Members page                    | Full page                   | wcag2a, wcag2aa, wcag21aa |
| Remove Member dialog                      | `[role="dialog"]`           | wcag2a, wcag2aa, wcag21aa |
| Workspace Sharing page (sharing enabled)  | Full page                   | wcag2a, wcag2aa, wcag21aa |
| Workspace Sharing page (sharing disabled) | Full page                   | wcag2a, wcag2aa, wcag21aa |
| Share Plugin dialog                       | `[role="dialog"]`           | wcag2a, wcag2aa, wcag21aa |

**Total: 7 axe-core Playwright test cases.** All pass with 0 violations.

---

**End of Design Specification — Workspace Management**

_Document Version: 1.1_
_Created: 2026-03-02_
_Updated: 2026-03-05 (§13 added — T9 accessibility audit results)_
_Author: forge-ux_
