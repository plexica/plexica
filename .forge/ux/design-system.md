# Plexica Design System

> **Generated by**: `/forge-ux`
> **Date**: 2026-03-08
> **Status**: Active
> **Version**: 1.8

This document defines the global design tokens and component guidelines for
the Plexica platform. All UI artifacts must reference these tokens.

---

## 1. Foundation

### Platform Context

Plexica uses **Tailwind CSS** with semantic CSS custom properties. The
project has a `@plexica/ui` component library with 30+ components. This
design system documents the token layer that underpins those components.

### Design Principles

1. **Consistency**: Reuse existing `@plexica/ui` components before creating new ones.
2. **Accessibility**: WCAG 2.1 AA compliance minimum. All tokens must support light and dark mode.
3. **Density**: Admin interfaces favor information-dense layouts. Use compact spacing.
4. **Clarity**: Status indicators, error states, and destructive actions must be unambiguous.

---

## 2. Colors

### Semantic Color Tokens

These tokens are the source of truth. They map to Tailwind's `--foreground`,
`--background`, etc. convention already in use in the codebase.

| Token                      | Light Mode | Dark Mode | Usage                                       |
| -------------------------- | ---------- | --------- | ------------------------------------------- |
| `--background`             | `#FFFFFF`  | `#0A0A0A` | Page background                             |
| `--foreground`             | `#0A0A0A`  | `#FAFAFA` | Primary text                                |
| `--card`                   | `#FFFFFF`  | `#111111` | Card backgrounds                            |
| `--card-foreground`        | `#0A0A0A`  | `#FAFAFA` | Card text                                   |
| `--muted`                  | `#F4F4F5`  | `#27272A` | Muted backgrounds (table headers, disabled) |
| `--muted-foreground`       | `#71717A`  | `#A1A1AA` | Secondary text, placeholders                |
| `--primary`                | `#0066CC`  | `#3B82F6` | Primary actions, links, active states       |
| `--primary-foreground`     | `#FFFFFF`  | `#FFFFFF` | Text on primary background                  |
| `--destructive`            | `#DC2626`  | `#EF4444` | Destructive actions, error states           |
| `--destructive-foreground` | `#FFFFFF`  | `#FFFFFF` | Text on destructive background              |
| `--border`                 | `#E4E4E7`  | `#27272A` | Borders, dividers                           |
| `--ring`                   | `#0066CC`  | `#3B82F6` | Focus ring                                  |

### Status Colors

| Token                       | Value (Light) | Value (Dark) | Usage                               |
| --------------------------- | ------------- | ------------ | ----------------------------------- |
| `--status-active`           | `#16A34A`     | `#22C55E`    | ACTIVE badge, success indicators    |
| `--status-suspended`        | `#DC2626`     | `#EF4444`    | SUSPENDED badge, error indicators   |
| `--status-provisioning`     | `#6B7280`     | `#9CA3AF`    | PROVISIONING badge, neutral/pending |
| `--status-pending-deletion` | `#D97706`     | `#F59E0B`    | PENDING_DELETION badge, warning     |
| `--status-deleted`          | `#6B7280`     | `#4B5563`    | DELETED badge, disabled             |
| `--status-success`          | `#16A34A`     | `#22C55E`    | Success toasts, checkmarks          |
| `--status-warning`          | `#D97706`     | `#F59E0B`    | Warning banners, caution            |
| `--status-info`             | `#2563EB`     | `#60A5FA`    | Informational banners               |

### Authentication-Specific Tokens (Spec 002)

| Token                     | Value (Light) | Value (Dark) | Usage                                                   |
| ------------------------- | ------------- | ------------ | ------------------------------------------------------- |
| `--auth-bg-gradient-from` | `#F0F4FF`     | `#0A0E1A`    | Subtle gradient on pre-login page (tenant-customizable) |
| `--auth-bg-gradient-to`   | `#FFFFFF`     | `#0A0A0A`    | Gradient end color (matches --background)               |

### Authorization-Specific Tokens (Spec 003)

| Token                    | Value (Light) | Value (Dark) | Usage                                                   |
| ------------------------ | ------------- | ------------ | ------------------------------------------------------- |
| `--condition-not-border` | `#D97706`     | `#F59E0B`    | Dashed border for NOT groups in ABAC condition builder  |
| `--condition-not-bg`     | `#FFFBEB`     | `#1C1917`    | Subtle background for NOT groups                        |
| `--permission-tree-line` | `#D4D4D8`     | `#3F3F46`    | Tree connector lines (├ └) for wildcard sub-permissions |

### Plugin System Tokens (Spec 004)

| Token                 | Value (Light)            | Value (Dark)              | Usage                                                                  |
| --------------------- | ------------------------ | ------------------------- | ---------------------------------------------------------------------- |
| `--status-installing` | `#6B7280`                | `#9CA3AF`                 | Semantic alias for `--status-provisioning` in plugin lifecycle context |
| `--plugin-icon-bg`    | `rgba(0, 102, 204, 0.1)` | `rgba(59, 130, 246, 0.1)` | Plugin icon background in registry cards (primary at 10% opacity)      |

### Frontend Shell Tokens (Spec 005)

| Token                     | Value (Light)     | Value (Dark)      | Usage                                                         |
| ------------------------- | ----------------- | ----------------- | ------------------------------------------------------------- |
| `--sidebar-width`         | `260px`           | `260px`           | Sidebar width on desktop                                      |
| `--sidebar-width-mobile`  | `280px`           | `280px`           | Sidebar overlay width on mobile/tablet                        |
| `--overlay-backdrop`      | `rgba(0,0,0,0.5)` | `rgba(0,0,0,0.7)` | Dimmed backdrop for sidebar overlay and session expired modal |
| `--header-height`         | `56px`            | `56px`            | Header bar height                                             |
| `--banner-warning-bg`     | `#FFFBEB`         | `#1C1917`         | Auth warning banner background                                |
| `--banner-warning-border` | `#FDE68A`         | `#92400E`         | Auth warning banner border                                    |
| `--skeleton-from`         | `#E5E7EB`         | `#1F2937`         | Skeleton loading animation start color                        |
| `--skeleton-to`           | `#F3F4F6`         | `#374151`         | Skeleton loading animation end color                          |

### i18n Tokens (Spec 006)

| Token                   | Value (Light) | Value (Dark) | Usage                                                                              |
| ----------------------- | ------------- | ------------ | ---------------------------------------------------------------------------------- |
| `--i18n-missing-border` | `#DC2626`     | `#EF4444`    | Semantic alias for `--destructive`. Dev-mode missing translation key dashed border |
| `--i18n-preview-bg`     | `#F8FAFC`     | `#1A1A2E`    | Preview panel background in Override Editor (distinct from `--muted`)              |
| `--i18n-original-bg`    | `#F0F4F8`     | `#1E1E2E`    | Original value read-only background (visual distinction from editable override)    |

### Core Services Tokens (Spec 007)

| Token                         | Value (Light) | Value (Dark) | Usage                                                                 |
| ----------------------------- | ------------- | ------------ | --------------------------------------------------------------------- |
| `--notification-dot`          | `#2563EB`     | `#60A5FA`    | Semantic alias for `--status-info`. Unread notification indicator dot |
| `--notification-dot-size`     | `8px`         | `8px`        | Diameter of unread indicator dot                                      |
| `--notification-badge-bg`     | `#DC2626`     | `#EF4444`    | Notification count badge background (red, on bell icon)               |
| `--notification-badge-fg`     | `#FFFFFF`     | `#FFFFFF`    | Notification count badge text (white on red)                          |
| `--search-overlay-width`      | `640px`       | `640px`      | Desktop width of search overlay                                       |
| `--search-overlay-max-height` | `480px`       | `480px`      | Max height before scrolling                                           |
| `--upload-progress-height`    | `8px`         | `8px`        | Height of upload progress bar                                         |
| `--upload-progress-bg`        | `#E5E7EB`     | `#374151`    | Progress bar track background                                         |
| `--upload-progress-fill`      | `#0066CC`     | `#3B82F6`    | Progress bar fill (semantic alias for --primary)                      |
| `--job-stat-card-min-width`   | `140px`       | `140px`      | Minimum width for stat cards on job dashboard                         |

### Admin Interface Tokens (Spec 008)

| Token                       | Value (Light) | Value (Dark) | Usage                                                                                                    |
| --------------------------- | ------------- | ------------ | -------------------------------------------------------------------------------------------------------- |
| `--admin-header-bg`         | `#FAFAFA`     | `#111111`    | Admin header background (slightly off from `--background` to create visual separation from content area) |
| `--admin-nav-active-border` | `#0066CC`     | `#3B82F6`    | Semantic alias for `--primary`. Left border on active sidebar nav item                                   |
| `--wizard-step-complete`    | `#16A34A`     | `#22C55E`    | Semantic alias for `--status-active`. Completed wizard step circle                                       |
| `--wizard-step-active`      | `#0066CC`     | `#3B82F6`    | Semantic alias for `--primary`. Current wizard step circle                                               |
| `--wizard-step-pending`     | `#D4D4D8`     | `#3F3F46`    | Pending wizard step circle (reuses border-adjacent gray)                                                 |
| `--wizard-step-error`       | `#DC2626`     | `#EF4444`    | Semantic alias for `--destructive`. Failed wizard step                                                   |
| `--provisioning-bar-bg`     | `#E5E7EB`     | `#374151`    | Semantic alias for `--upload-progress-bg`. Provisioning progress bar track                               |
| `--provisioning-bar-fill`   | `#0066CC`     | `#3B82F6`    | Semantic alias for `--primary`. Provisioning progress bar fill                                           |

### Frontend Production Readiness Tokens (Spec 010)

| Token                         | Value (Light) | Value (Dark) | Usage                                                                              |
| ----------------------------- | ------------- | ------------ | ---------------------------------------------------------------------------------- |
| `--error-boundary-icon`       | `#D97706`     | `#F59E0B`    | Semantic alias for `--status-warning`. Warning icon color in plugin error fallback |
| `--error-boundary-bg`         | `#FFFFFF`     | `#111111`    | Semantic alias for `--card`. Error fallback card background                        |
| `--widget-placeholder-border` | `#E4E4E7`     | `#27272A`    | Semantic alias for `--border`. Widget fallback dashed border                       |
| `--widget-placeholder-icon`   | `#71717A`     | `#A1A1AA`    | Semantic alias for `--muted-foreground`. Widget fallback icon color                |
| `--root-error-bg`             | `#FFFFFF`     | `#0A0A0A`    | Semantic alias for `--background`. Root error page background                      |

> These are semantic aliases of existing tokens, introduced so error boundary
> and widget fallback styling can be independently adjusted without affecting
> base tokens. See design-spec.md §5 for rationale.

### Workspace Hierarchy & Templates Tokens (Spec 011)

| Token                       | Value (Light) | Value (Dark) | Usage                                                                     |
| --------------------------- | ------------- | ------------ | ------------------------------------------------------------------------- |
| `--tree-indent`             | `16px`        | `16px`       | Per-level indentation for workspace tree (depth × token)                  |
| `--tree-connector-color`    | `#D4D4D8`     | `#3F3F46`    | Semantic alias for `--permission-tree-line`. Tree connector lines (├ └ │) |
| `--tree-connector-width`    | `1px`         | `1px`        | Stroke width of tree connector lines                                      |
| `--tree-node-height`        | `36px`        | `36px`       | Minimum height of a tree node row (desktop)                               |
| `--tree-node-height-mobile` | `44px`        | `44px`       | Minimum height of a tree node row (mobile, per WCAG 2.5.5 target size)    |
| `--tree-selected-border`    | `3px`         | `3px`        | Left border width on selected tree node                                   |
| `--template-card-min-width` | `180px`       | `180px`      | Minimum card width in template picker grid                                |
| `--template-card-selected`  | `#0066CC`     | `#3B82F6`    | Semantic alias for `--ring`. Selected template card ring color            |
| `--template-card-check-bg`  | `#0066CC`     | `#3B82F6`    | Semantic alias for `--primary`. Check overlay background on selected card |

> Tree connector tokens reuse the same values as `--permission-tree-line`
> (Spec 003) for visual consistency. They are defined as separate aliases so
> hierarchy and ABAC tree styling can evolve independently. Mobile node height
> follows WCAG 2.5.5 (44×44px minimum target size) for touch accessibility.

### Plugin Observability Tokens (Spec 012)

| Token                           | Value (Light) | Value (Dark) | Usage                                                    |
| ------------------------------- | ------------- | ------------ | -------------------------------------------------------- |
| `--obs-healthy`                 | `#16A34A`     | `#22C55E`    | Alias `--status-active`. Healthy badge/bar               |
| `--obs-degraded`                | `#D97706`     | `#F59E0B`    | Alias `--status-warning`. Degraded badge/bar             |
| `--obs-down`                    | `#DC2626`     | `#EF4444`    | Alias `--destructive`. Down badge/bar                    |
| `--obs-unknown`                 | `#6B7280`     | `#9CA3AF`    | Alias `--status-provisioning`. Unknown badge             |
| `--obs-alert-critical-border`   | `#DC2626`     | `#EF4444`    | Alias `--destructive`. Critical alert card left border   |
| `--obs-alert-warning-border`    | `#D97706`     | `#F59E0B`    | Alias `--status-warning`. Warning alert card left border |
| `--obs-span-bar-bg`             | `#0066CC`     | `#3B82F6`    | Alias `--primary`. Default span bar color                |
| `--obs-span-bar-error`          | `#DC2626`     | `#EF4444`    | Alias `--destructive`. Error span bar color              |
| `--obs-chart-series-1`          | `#16A34A`     | `#22C55E`    | Chart series: 2xx responses (green)                      |
| `--obs-chart-series-2`          | `#D97706`     | `#F59E0B`    | Chart series: 4xx responses (yellow)                     |
| `--obs-chart-series-3`          | `#DC2626`     | `#EF4444`    | Chart series: 5xx responses (red)                        |
| `--obs-chart-series-p50`        | `#0066CC`     | `#3B82F6`    | Chart series: P50 latency line                           |
| `--obs-chart-series-p95`        | `#D97706`     | `#F59E0B`    | Chart series: P95 latency line                           |
| `--obs-chart-series-p99`        | `#DC2626`     | `#EF4444`    | Chart series: P99 latency line                           |
| `--obs-chart-series-memory`     | `#7C3AED`     | `#A78BFA`    | Chart series: memory usage (purple)                      |
| `--obs-chart-series-cpu`        | `#0891B2`     | `#22D3EE`    | Chart series: CPU usage (cyan)                           |
| `--obs-waterfall-indent`        | `24px`        | `24px`       | Per-level indentation in span waterfall                  |
| `--obs-waterfall-bar-height`    | `20px`        | `20px`       | Height of each span bar                                  |
| `--obs-waterfall-row-height`    | `48px`        | `48px`       | Row height in waterfall (≥44px for WCAG 2.5.5)           |
| `--obs-chart-min-height`        | `240px`       | `240px`      | Minimum height for chart panels                          |
| `--obs-alert-card-border-width` | `4px`         | `4px`        | Left border width on alert cards                         |

> Semantic aliases allowing observability styling to evolve independently.
> Chart series colors all pass WCAG 2.1 AA 3:1 minimum contrast for non-text
> elements against the chart background (`--card`).

### Extension Points Tokens (Spec 013)

| Token                          | Value (Light)          | Value (Dark)            | Usage                                                                     |
| ------------------------------ | ---------------------- | ----------------------- | ------------------------------------------------------------------------- |
| `--ext-slot-border`            | `#E4E4E7`              | `#27272A`               | Semantic alias for `--border`. Subtle border around extension slot region |
| `--ext-slot-border-inspector`  | `#0066CC`              | `#3B82F6`               | Semantic alias for `--primary`. Dashed border in dev inspector overlay    |
| `--ext-badge-bg`               | `rgba(0,102,204,0.08)` | `rgba(59,130,246,0.12)` | Background of hover-visible extension badge (primary at 8% opacity)       |
| `--ext-badge-icon`             | `#71717A`              | `#A1A1AA`               | Semantic alias for `--muted-foreground`. Puzzle icon color                |
| `--ext-error-bg`               | `#FFFFFF`              | `#111111`               | Semantic alias for `--card`. Error fallback card background               |
| `--ext-error-icon`             | `#D97706`              | `#F59E0B`               | Semantic alias for `--status-warning`. AlertTriangle icon color           |
| `--ext-permission-granted`     | `#16A34A`              | `#22C55E`               | Semantic alias for `--status-active`. "Granted" badge color               |
| `--ext-permission-not-granted` | `#6B7280`              | `#9CA3AF`               | Semantic alias for `--status-provisioning`. "Not Granted" badge color     |
| `--ext-highlight-new`          | `rgba(217,119,6,0.1)`  | `rgba(245,158,11,0.1)`  | New permission row highlight flash (warning at 10% opacity)               |
| `--ext-contribution-gap`       | `12px`                 | `12px`                  | Semantic alias for `--space-3`. Gap between contributions in a slot       |
| `--ext-inspector-backdrop`     | `rgba(0,0,0,0.3)`      | `rgba(0,0,0,0.5)`       | Dev inspector overlay backdrop                                            |

> Semantic aliases for the extension points feature. Extension slot borders
> and badge colors can evolve independently from base layout tokens.

### Layout Engine Tokens (Spec 014)

| Token                          | Value (Light) | Value (Dark) | Usage                                                                         |
| ------------------------------ | ------------- | ------------ | ----------------------------------------------------------------------------- |
| `--layout-toggle-editable`     | `#16A34A`     | `#22C55E`    | Semantic alias for `--status-active`. ✓ₑ toggle icon color                    |
| `--layout-toggle-readonly`     | `#2563EB`     | `#60A5FA`    | Semantic alias for `--status-info`. ✓ᵣ toggle icon color                      |
| `--layout-toggle-hidden`       | `#DC2626`     | `#EF4444`    | Semantic alias for `--status-suspended`. ✗ toggle icon color                  |
| `--layout-required-indicator`  | `#D97706`     | `#F59E0B`    | Semantic alias for `--status-warning`. ⚠ indicator for required fields        |
| `--layout-stale-badge-bg`      | `#FFFBEB`     | `#1C1917`    | Semantic alias for `--banner-warning-bg`. Stale field badge background        |
| `--layout-stale-badge-fg`      | `#D97706`     | `#F59E0B`    | Semantic alias for `--status-warning`. Stale field badge text color           |
| `--layout-preview-bg`          | `#F8FAFC`     | `#1A1A2E`    | Semantic alias for `--i18n-preview-bg`. Preview panel background              |
| `--layout-readonly-field-bg`   | `#F4F4F5`     | `#27272A`    | Semantic alias for `--muted`. Read-only field background in forms             |
| `--layout-toggle-size`         | `32px`        | `32px`       | Size of toggle button in config table (padded to 44px touch target on mobile) |
| `--layout-grid-cell-min-width` | `72px`        | `72px`       | Minimum width for role visibility columns in config table                     |

> Semantic aliases following the pattern from Specs 006, 010, and 011.
> Layout engine styling can evolve independently without affecting other features.

### Contrast Verification (WCAG 2.1 AA)

| Pair                                                   | Ratio (Light) | Ratio (Dark) | Pass |
| ------------------------------------------------------ | ------------- | ------------ | ---- |
| `--foreground` on `--background`                       | 17.4:1        | 17.4:1       | ✅   |
| `--muted-foreground` on `--background`                 | 4.7:1         | 4.6:1        | ✅   |
| `--primary-foreground` on `--primary`                  | 5.3:1         | 4.6:1        | ✅   |
| `--destructive-foreground` on `--destructive`          | 5.8:1         | 4.6:1        | ✅   |
| `--status-active` text on `--background`               | 4.5:1         | 4.6:1        | ✅   |
| `--status-warning` text on `--background`              | 4.5:1         | 4.5:1        | ✅   |
| `--notification-badge-fg` on `--notification-badge-bg` | 5.8:1         | 4.6:1        | ✅   |
| `--notification-dot` on `--card`                       | 5.3:1         | 4.7:1        | ✅   |
| `--foreground` on `--admin-header-bg`                  | 16.1:1        | 17.4:1       | ✅   |
| `--wizard-step-complete` (text on white)               | 4.5:1         | 4.6:1        | ✅   |
| `--wizard-step-active` (text on white)                 | 5.3:1         | 4.6:1        | ✅   |
| `--wizard-step-error` (text on white)                  | 5.8:1         | 4.6:1        | ✅   |
| `--error-boundary-icon` on `--error-boundary-bg`       | 4.5:1         | 4.5:1        | ✅   |
| `--widget-placeholder-icon` on `--card`                | 4.7:1         | 4.6:1        | ✅   |
| `--foreground` on `--root-error-bg`                    | 17.4:1        | 17.4:1       | ✅   |
| `--obs-healthy` text on `--background`                 | 4.5:1         | 4.6:1        | ✅   |
| `--obs-degraded` text on `--background`                | 4.5:1         | 4.5:1        | ✅   |
| `--obs-down` text on `--background`                    | 5.8:1         | 4.6:1        | ✅   |
| `--obs-chart-series-memory` on `--card`                | 6.5:1         | 4.5:1        | ✅   |
| `--obs-chart-series-cpu` on `--card`                   | 4.5:1         | 4.5:1        | ✅   |
| `--obs-chart-series-p50` on `--card`                   | 5.3:1         | 4.6:1        | ✅   |
| `--ext-badge-icon` on `--card`                         | 4.7:1         | 4.6:1        | ✅   |
| `--ext-error-icon` on `--ext-error-bg`                 | 4.5:1         | 4.5:1        | ✅   |
| `--ext-permission-granted` on `--background`           | 4.5:1         | 4.6:1        | ✅   |
| `--ext-permission-not-granted` on `--background`       | 4.8:1         | 4.5:1        | ✅   |
| `--foreground` on `--ext-highlight-new`                | 16.5:1        | 16.2:1       | ✅   |
| `--layout-toggle-editable` icon on `--card`            | 4.5:1         | 4.6:1        | ✅   |
| `--layout-toggle-readonly` icon on `--card`            | 5.3:1         | 4.7:1        | ✅   |
| `--layout-toggle-hidden` icon on `--card`              | 5.8:1         | 4.6:1        | ✅   |
| `--layout-required-indicator` on `--card`              | 4.5:1         | 4.5:1        | ✅   |
| `--layout-stale-badge-fg` on `--layout-stale-badge-bg` | 4.5:1         | 4.5:1        | ✅   |
| `--foreground` on `--layout-preview-bg`                | 16.1:1        | 17.0:1       | ✅   |
| `--foreground` on `--layout-readonly-field-bg`         | 14.2:1        | 14.2:1       | ✅   |

---

## 3. Typography

### Scale

| Token                   | Value           | Usage                                 |
| ----------------------- | --------------- | ------------------------------------- |
| `--font-size-xs`        | 12px / 0.75rem  | Captions, helper text, timestamps     |
| `--font-size-sm`        | 14px / 0.875rem | Table cells, secondary labels, badges |
| `--font-size-base`      | 16px / 1rem     | Body text, form inputs                |
| `--font-size-lg`        | 18px / 1.125rem | Card titles, subheadings              |
| `--font-size-xl`        | 20px / 1.25rem  | Section headings                      |
| `--font-size-2xl`       | 24px / 1.5rem   | Page titles (h2)                      |
| `--font-size-3xl`       | 30px / 1.875rem | Hero headings (h1)                    |
| `--line-height-tight`   | 1.25            | Headings                              |
| `--line-height-normal`  | 1.5             | Body text                             |
| `--line-height-relaxed` | 1.75            | Long-form content                     |

### Font Families

| Token         | Value                                            | Usage                                  |
| ------------- | ------------------------------------------------ | -------------------------------------- |
| `--font-sans` | `Inter, system-ui, -apple-system, sans-serif`    | All UI text                            |
| `--font-mono` | `JetBrains Mono, Fira Code, Consolas, monospace` | Code, slugs, IDs, infrastructure names |

### Font Weights

| Token             | Value | Usage                         |
| ----------------- | ----- | ----------------------------- |
| `--font-normal`   | 400   | Body text                     |
| `--font-medium`   | 500   | Labels, table headers         |
| `--font-semibold` | 600   | Card titles, section headings |
| `--font-bold`     | 700   | Page titles, emphasis         |

---

## 4. Spacing

| Token         | Value | Usage                                       |
| ------------- | ----- | ------------------------------------------- |
| `--space-0.5` | 2px   | Micro gaps (icon-to-text tight)             |
| `--space-1`   | 4px   | Inline icon gaps, badge padding             |
| `--space-1.5` | 6px   | Compact input padding                       |
| `--space-2`   | 8px   | Icon gaps, tight stacking, button padding-y |
| `--space-3`   | 12px  | Compact card padding, input padding-x       |
| `--space-4`   | 16px  | Default card padding, form spacing          |
| `--space-5`   | 20px  | Modal padding                               |
| `--space-6`   | 24px  | Section spacing within a page               |
| `--space-8`   | 32px  | Between major sections                      |
| `--space-10`  | 40px  | Page-level vertical spacing                 |
| `--space-12`  | 48px  | Empty state vertical padding                |

---

## 5. Border Radius

| Token           | Value  | Usage                            |
| --------------- | ------ | -------------------------------- |
| `--radius-sm`   | 4px    | Inputs, badges, tags             |
| `--radius-md`   | 6px    | Buttons, cards                   |
| `--radius-lg`   | 8px    | Modals, panels                   |
| `--radius-xl`   | 12px   | Large cards, featured content    |
| `--radius-full` | 9999px | Avatars, pills, circular buttons |

---

## 6. Shadows

| Token         | Value                                                               | Usage                  |
| ------------- | ------------------------------------------------------------------- | ---------------------- |
| `--shadow-sm` | `0 1px 2px rgba(0,0,0,0.05)`                                        | Subtle lift for inputs |
| `--shadow-md` | `0 4px 6px -1px rgba(0,0,0,0.07), 0 2px 4px -2px rgba(0,0,0,0.05)`  | Cards, dropdowns       |
| `--shadow-lg` | `0 10px 15px -3px rgba(0,0,0,0.1), 0 4px 6px -4px rgba(0,0,0,0.1)`  | Modals, overlays       |
| `--shadow-xl` | `0 20px 25px -5px rgba(0,0,0,0.1), 0 8px 10px -6px rgba(0,0,0,0.1)` | Floating panels        |

---

## 7. Transitions

| Token                 | Value               | Usage                              |
| --------------------- | ------------------- | ---------------------------------- |
| `--transition-fast`   | `150ms ease-in-out` | Hover states, color changes        |
| `--transition-normal` | `200ms ease-in-out` | Accordion, tab switching           |
| `--transition-slow`   | `300ms ease-in-out` | Sidebar collapse, modal open/close |

---

## 8. Breakpoints

| Token | Value  | Usage                            |
| ----- | ------ | -------------------------------- |
| `sm`  | 640px  | Mobile landscape                 |
| `md`  | 768px  | Tablet portrait                  |
| `lg`  | 1024px | Tablet landscape / small desktop |
| `xl`  | 1280px | Standard desktop                 |
| `2xl` | 1440px | Large desktop                    |

**Primary design targets**:

- Super Admin panel: Desktop-first (1440px → 1024px → 768px)
- Tenant web app: Desktop-first with responsive support (1440px → 768px → 375px)

---

## 9. Component Guidelines

### Existing `@plexica/ui` Components

The following components are available and should be reused:

| Component                                                                | Variants                                                | Notes                               |
| ------------------------------------------------------------------------ | ------------------------------------------------------- | ----------------------------------- |
| `Button`                                                                 | primary, secondary, outline, ghost, destructive, danger | Sizes: sm, md, lg                   |
| `Input`                                                                  | default, error                                          | With optional label                 |
| `Label`                                                                  | default                                                 | Associates with input via `htmlFor` |
| `Badge`                                                                  | default, secondary, outline, danger                     | For status indicators               |
| `Card`, `CardHeader`, `CardContent`, `CardTitle`                         | default                                                 | Container component                 |
| `Modal`                                                                  | default                                                 | With focus trap                     |
| `Alert`, `AlertTitle`, `AlertDescription`                                | default, destructive                                    | For banners                         |
| `Checkbox`                                                               | default                                                 | For plugin selection                |
| `Switch`                                                                 | default                                                 | For toggle settings                 |
| `Select`                                                                 | default                                                 | Dropdown selection                  |
| `Progress`                                                               | default                                                 | Progress bars                       |
| `Skeleton`                                                               | default                                                 | Loading placeholders                |
| `EmptyState`                                                             | default                                                 | Empty data states                   |
| `DataTable`                                                              | default                                                 | Sortable/filterable tables          |
| `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent`                         | default                                                 | Tab navigation                      |
| `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogFooter` | default                                                 | Modal dialogs                       |
| `Toast`                                                                  | success, error, warning, info                           | Notification toasts                 |
| `Tooltip`                                                                | default                                                 | Hover tooltips                      |
| `Spinner`                                                                | default                                                 | Loading spinners                    |
| `Pagination`                                                             | default                                                 | Page navigation                     |
| `Breadcrumbs`                                                            | default                                                 | Navigation breadcrumbs              |
| `Separator`                                                              | default                                                 | Visual dividers                     |
| `Textarea`                                                               | default                                                 | Multi-line text input               |
| `Avatar`                                                                 | default                                                 | User avatars                        |
| `StatusBadge`                                                            | default                                                 | Status indicators                   |
| `StatCard`                                                               | default                                                 | Metric cards                        |

### New Components Needed (Spec 001)

| Component              | Purpose                                                   | Screens Used                                   |
| ---------------------- | --------------------------------------------------------- | ---------------------------------------------- |
| `StepWizard`           | Multi-step form with step indicator, back/next navigation | Tenant Creation                                |
| `ProvisioningProgress` | Real-time provisioning status with per-step indicators    | Tenant Creation                                |
| `ColorPicker`          | Hex color input with visual swatch preview                | Tenant Creation (theme), Tenant Admin Branding |
| `DeletionCountdown`    | Displays remaining days/hours until permanent deletion    | Tenant Detail, Tenant List                     |
| `ThemePreview`         | Mini app shell showing live theme preview                 | Tenant Admin Branding                          |

### New Components Needed (Spec 002)

| Component             | Purpose                                                                   | Screens Used                            |
| --------------------- | ------------------------------------------------------------------------- | --------------------------------------- |
| `SessionExpiredModal` | Non-dismissable modal overlay prompting re-authentication on token expiry | Any authenticated page (app shell)      |
| `RateLimitCountdown`  | Warning alert with live countdown timer for rate-limited login attempts   | Pre-Login Landing Page                  |
| `AuthErrorPage`       | Full-screen error layout for tenant not found / suspended / Keycloak down | Pre-Login Landing Page (error variants) |

### New Components Needed (Spec 003)

| Component                  | Purpose                                                                 | Screens Used                            |
| -------------------------- | ----------------------------------------------------------------------- | --------------------------------------- |
| `PermissionGroupAccordion` | Collapsible permission group labeled by source (Core / Plugin name)     | Role Editor, Role Detail                |
| `WildcardPermissionRow`    | Checkbox with auto-select/deselect of sub-permissions                   | Role Editor                             |
| `ConditionBuilder`         | Visual nested AND/OR/NOT condition tree editor for ABAC policies        | ABAC Policy Editor                      |
| `ConditionRow`             | Single condition row (attribute, operator, value) within condition tree | ABAC Policy Editor                      |
| `PolicySummary`            | Auto-generated plain-English summary of ABAC policy conditions          | ABAC Policy Editor                      |
| `RoleAssignmentDialog`     | Dialog for assigning/removing roles with change diff preview            | User Role Assignment                    |
| `EffectBadge`              | Colored badge for ABAC policy effect (DENY = red, FILTER = blue)        | Policy List, Policy Editor              |
| `SystemRoleBadge`          | Lock icon + "System" badge for immutable system roles                   | Role List, Role Detail, Role Assignment |

### New Components Needed (Spec 004)

| Component               | Purpose                                                                               | Screens Used                                           |
| ----------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| `PluginStatusBadge`     | Lifecycle-aware status badge mapping 7 statuses to colors/icons (extends StatusBadge) | Plugin Registry, Plugin Detail View                    |
| `InstallProgressPanel`  | Step-by-step installation progress with per-tenant migration detail                   | Plugin Install Progress                                |
| `LifecycleTimeline`     | Horizontal stepper showing plugin lifecycle history with timestamps                   | Plugin Detail View                                     |
| `PluginHealthIndicator` | Health check status indicator (healthy/unhealthy/unknown) with response time          | Plugin Registry cards, Plugin Detail View (Health tab) |
| `SchemaForm`            | JSON Schema → auto-generated form with full type support                              | Tenant Plugin Settings                                 |
| `SchemaField`           | Single form field renderer based on JSON Schema type                                  | Tenant Plugin Settings (sub-component of SchemaForm)   |
| `SchemaFieldset`        | Collapsible fieldset for nested JSON Schema objects                                   | Tenant Plugin Settings (sub-component of SchemaForm)   |
| `TagInput`              | Tag editor for `array` of `string` schema fields (add/remove pills)                   | Tenant Plugin Settings (sub-component of SchemaForm)   |
| `RepeatableFieldGroup`  | Add/remove rows for `array` of `object` schema fields                                 | Tenant Plugin Settings (sub-component of SchemaForm)   |
| `ConditionalFields`     | Show/hide fields based on `oneOf`/`anyOf` discriminator                               | Tenant Plugin Settings (sub-component of SchemaForm)   |
| `RegisterPluginModal`   | Modal with JSON editor, manifest validation, and registration flow                    | Plugin Registry                                        |

### New Components Needed (Spec 005)

| Component               | Purpose                                                                                                                                | Screens Used                                      |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| `SidebarNav`            | App shell sidebar with core nav items + collapsible plugins group                                                                      | All shell pages                                   |
| `PluginLoadingBoundary` | ~~Replaced by `PluginErrorBoundary` + `PluginErrorFallback` (Spec 010)~~ — Error boundary + Suspense wrapper for remote module loading | Plugin pages, widget embeds                       |
| `AuthWarningBanner`     | Persistent yellow banner warning about token refresh failures                                                                          | Below header, any page                            |
| `PluginNotFoundPage`    | 404 page for disabled plugin routes                                                                                                    | Disabled plugin route navigation                  |
| `WidgetContainer`       | Section wrapper for embedding cross-plugin widgets with loading/error states                                                           | Plugin pages with cross-plugin widget integration |
| `ColorPickerField`      | Extended ColorPicker with contrast ratio indicator and validation                                                                      | Tenant Theme Settings                             |

### New Components Needed (Spec 006)

| Component              | Purpose                                                                             | Screens Used                                             |
| ---------------------- | ----------------------------------------------------------------------------------- | -------------------------------------------------------- |
| `LanguageSelector`     | Globe icon dropdown showing available locales in native script (no flags)           | App Shell Header (all pages), User Profile Settings      |
| `TranslationKeyTable`  | Extends `DataTable` with expandable rows, namespace chips, orphaned key indicators  | Translation Override Editor                              |
| `OverrideInlineEditor` | Inline form for editing overrides with original/override side-by-side + ICU preview | Translation Override Editor (expanded row)               |
| `MissingKeyHighlight`  | Dev-mode wrapper with red dashed border, tooltip with key path + copy button        | Any page (development mode only, `NODE_ENV=development`) |
| `OrphanedKeyBadge`     | Warning badge variant with `AlertTriangle` + tooltip for orphaned override keys     | Translation Override Editor (extends existing `Badge`)   |

### New Components Needed (Spec 007)

| Component          | Purpose                                                                                   | Screens Used                             |
| ------------------ | ----------------------------------------------------------------------------------------- | ---------------------------------------- |
| `SearchOverlay`    | Modal search overlay with combobox, grouped results by type, keyboard navigation          | Shell Header → Global Search (all pages) |
| `NotificationBell` | Bell icon button with unread badge + dropdown menu for in-app notifications (SSE-aware)   | Shell Header (all pages)                 |
| `FileUploadZone`   | Drag-and-drop + click-to-upload area with progress bar, cancel, retry, size validation    | Plugin pages with file attachments       |
| `FileListItem`     | File row with name, size, date, download (signed URL), and delete actions                 | Plugin pages with file attachments       |
| `JobDetailPanel`   | Expandable inline detail panel for job table rows with metadata and retry/disable actions | Job Status Dashboard (Admin)             |
| `JobStatusBadge`   | Extends `StatusBadge` with 7 job-specific statuses (Pending→Completed) and icons          | Job Status Dashboard (Admin)             |

### New Components Needed (Spec 008)

| Component                 | Purpose                                                                                                                      | Screens Used                                                                  |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| `AdminSidebarNav`         | Admin shell sidebar with two variants: `super-admin` (7 items) and `tenant-admin` (7 items); collapses to hamburger at 768px | All admin screens (SA + TA)                                                   |
| `TenantStatusBadge`       | Extends `StatusBadge` with 5 tenant lifecycle statuses (ACTIVE→DELETED) and color tokens                                     | SA Tenant List, SA Tenant Detail                                              |
| `ProvisioningWizard`      | 3-step wizard (Details → Configure → Provisioning) with real-time SSE progress on Step 3                                     | SA Tenant Create                                                              |
| `AuditLogTable`           | Extends `DataTable` with date range filter, action/tenant/user filters, 50-row pagination, and 10k-cap warning               | SA Global Audit Log, TA Tenant Audit Log                                      |
| `SystemHealthCard`        | Service health card with status, latency, uptime; `compact` and `detailed` variants; auto-refresh 30s                        | SA Dashboard, SA System Health                                                |
| `DestructiveConfirmModal` | Extends `Dialog` with `typed-confirm` (type slug to delete) and `simple-confirm` variants; shows consequences as bullet list | SA Tenant Delete/Suspend, SA Admin Remove, TA User Deactivate, TA Team Delete |

### New Components Needed (Spec 009)

| Component                   | Purpose                                                                                                                           | Screens Used                         |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ |
| `WorkspaceSettingsForm`     | Typed 5-field settings form (Select, Switch×2, Input number) per Appendix D schema with Zod validation                            | Workspace Settings (General tab)     |
| `SharePluginDialog`         | Multi-select dialog with dual searchable checkbox lists (plugin + target workspace) for cross-workspace sharing                   | Resource Sharing tab (Settings page) |
| `SharedResourceRow`         | List item displaying a shared resource with plugin icon, name, shared-with/from info, sharer, date, type badge, and revoke action | Resource Sharing tab (Settings page) |
| `RevokeShareDialog`         | Confirmation dialog naming the specific plugin + target workspace before revoking a share                                         | Resource Sharing tab (Settings page) |
| `SharingDisabledEmptyState` | Full-tab empty state with Lock icon and CTA to Settings when `allowCrossWorkspaceSharing` is false                                | Resource Sharing tab (Settings page) |

### New Components Needed (Spec 010)

| Component               | Purpose                                                                                                                                | Screens Used                                   |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| `PluginErrorBoundary`   | React error boundary wrapping each plugin route; catches crashes without affecting shell/sidebar; supports retry via reset             | All plugin route pages                         |
| `PluginErrorFallback`   | Presentational fallback card with warning icon, plugin name, error summary, [Retry] and [Go Back] buttons                              | Plugin Error Fallback screen                   |
| `RootErrorBoundary`     | Top-level error boundary wrapping entire app; full-page fallback with Plexica logo, [Reload Page], and support contact                 | Root Error Page (shell-level crash)            |
| `ThemeProvider`         | React Context provider that fetches tenant theme from API, validates colors/fonts, sets CSS custom properties on `:root`               | All authenticated pages (wraps shell)          |
| `TenantLogo`            | Image component with automatic fallback to Plexica wordmark on load error or missing tenant logo                                       | Shell header                                   |
| `WidgetLoader`          | Suspense + lazy wrapper for Module Federation widget loading; orchestrates loading skeleton, rendered widget, and unavailable fallback | Any plugin page embedding cross-plugin widgets |
| `WidgetFallback`        | Non-interactive dashed-border placeholder shown when a widget remote fails to load; no retry button                                    | Widget Unavailable state                       |
| `WidgetLoadingSkeleton` | Animated skeleton bars (pulse effect) shown during widget lazy load; uses existing `Skeleton` component from `@plexica/ui`             | Widget Loading state                           |

### New Components Needed (Spec 011)

| Component             | Purpose                                                                                                        | Screens Used                                        |
| --------------------- | -------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| `WorkspaceTreeView`   | Hierarchical tree with expand/collapse, ARIA tree roles, keyboard nav; `compact` (sidebar) and `table` (admin) | Workspace Switcher, Admin Hierarchy Tree View       |
| `WorkspaceTreeNode`   | Individual tree node row with indent, expand toggle, name link, and aggregation columns (table variant)        | Workspace Switcher, Admin Hierarchy Tree View       |
| `TemplatePickerGrid`  | CSS Grid of template cards with single-select, keyboard navigation, and empty state                            | Create Workspace Dialog                             |
| `TemplateCard`        | Selectable card with check overlay, name, description, plugin count, and workspace count badges                | Create Workspace Dialog (inside TemplatePickerGrid) |
| `PluginToggleCard`    | Plugin card with Switch toggle for enabling/disabling plugins from a workspace template                        | Workspace Settings (Plugins tab)                    |
| `MoveWorkspaceDialog` | Dialog with parent-workspace dropdown, impact preview, and confirmation for reparenting a workspace            | Admin Hierarchy Tree View                           |

### New Components Needed (Spec 012)

| Component               | Purpose                                                                 | Screens Used                     |
| ----------------------- | ----------------------------------------------------------------------- | -------------------------------- |
| `HealthStatusBadge`     | Status badge with 4 states (Healthy/Degraded/Down/Unknown) + icon       | Health tab, Sidebar (mini badge) |
| `HealthSummaryTable`    | Auto-refreshing plugin health data table with severity sorting          | Health tab                       |
| `MetricsChartPanel`     | Chart wrapper with accessible fallback, loading/error states            | Metrics tab                      |
| `TimeRangeSelector`     | Toggle button group for 1h/6h/24h/7d time range selection               | Metrics tab, Traces tab          |
| `TraceSearchForm`       | Search form: service dropdown + trace ID + time range + submit          | Traces tab                       |
| `TraceResultsTable`     | Sortable trace results table with clickable rows                        | Traces tab                       |
| `SpanWaterfall`         | Tree-structured horizontal bar chart showing trace span hierarchy       | Trace Detail                     |
| `SpanDetailPanel`       | Span attributes display panel (key-value table)                         | Trace Detail                     |
| `ActiveAlertCard`       | Alert card with severity styling, description, timestamp, action button | Alerts tab                       |
| `AlertHistoryTable`     | Paginated table of resolved alerts with severity filter                 | Alerts tab                       |
| `AutoRefreshIndicator`  | Spinning icon + "Last updated" timestamp for polling data               | Health tab                       |
| `AccessibleChartToggle` | Toggle button that shows a DataTable alternative view of chart data     | Metrics tab                      |
| `ObservabilityPage`     | Page-level component with tab routing and shared state                  | All observability screens        |

### New Components Needed (Spec 013)

| Component                  | Purpose                                                                                | Screens Used                                   |
| -------------------------- | -------------------------------------------------------------------------------------- | ---------------------------------------------- |
| `ExtensionSlot`            | Container that queries registry, loads contributions via Module Federation, renders    | Plugin pages with extension slots              |
| `ExtensionContribution`    | Error boundary wrapper for individual contributed remote components                    | Plugin pages with extension slots              |
| `ExtensionSlotSkeleton`    | Type-aware loading skeleton (action/panel/form/toolbar variants)                       | Plugin pages with extension slots              |
| `ExtensionErrorFallback`   | Compact/card error fallback when a contribution fails (extends error boundary pattern) | Plugin pages with extension slots              |
| `ExtensionSettingsPanel`   | Workspace admin accordion-based extension visibility settings page                     | Workspace Settings (Extensions tab)            |
| `ContributionRow`          | Individual contribution toggle row within extension settings accordion                 | Workspace Settings (Extensions tab)            |
| `ExtensionPermissionsPage` | Tenant admin DataTable for managing cross-plugin extension permissions                 | Tenant Admin (Extension Permissions page)      |
| `SlotInspectorOverlay`     | Dev-mode overlay highlighting extension slots with metadata (Ctrl+Shift+E)             | Any page (development mode only)               |
| `VirtualizedSlotContainer` | Virtual scroll wrapper for slots with >20 contributions                                | Plugin pages with many extension contributions |

### New Components Needed (Spec 014)

| Component                    | Purpose                                                                                 | Screens Used                              |
| ---------------------------- | --------------------------------------------------------------------------------------- | ----------------------------------------- |
| `LayoutConfigPanel`          | Admin page for layout configuration (field order, visibility, sections, columns)        | Layout Configuration Admin page           |
| `FieldConfigTable`           | Grid table for field ordering and per-role visibility (tri-state toggle per cell)       | Layout Configuration Admin page           |
| `ColumnConfigTable`          | Grid table for column per-role visibility (bi-state toggle per cell)                    | Layout Configuration Admin page           |
| `SectionOrderList`           | Reorderable list for section ordering with up/down arrow controls                       | Layout Configuration Admin page           |
| `VisibilityToggle`           | Tri-state (editable → read-only → hidden) or bi-state (visible → hidden) cycling button | Layout Configuration Admin page           |
| `RolePreviewPanel`           | Read-only form preview for a selected role (client-side computed, no API call)          | Layout Configuration Admin page           |
| `RequiredFieldWarningDialog` | Modal confirmation for hiding required fields without default values                    | Layout Configuration (on save)            |
| `LayoutAwareForm`            | Wrapper applying layout transformations (field order, visibility) to plugin forms       | Any plugin form page                      |
| `LayoutAwareTable`           | Wrapper applying column visibility/ordering to plugin data tables                       | Any plugin list/table view                |
| `useResolvedLayout`          | React Query hook for fetching and caching resolved layout config per form/workspace     | Used by LayoutAwareForm, LayoutAwareTable |

---

## 10. Icon Library

**Icon set**: [Lucide React](https://lucide.dev/) (already in use across codebase)

| Icon             | Lucide Name                   | Usage                                                 |
| ---------------- | ----------------------------- | ----------------------------------------------------- |
| Building         | `Building2`                   | Tenant icon                                           |
| Puzzle piece     | `Puzzle`                      | Plugin icon                                           |
| Check circle     | `CheckCircle2`                | Success / valid indicator                             |
| Alert circle     | `AlertCircle`                 | Error / info indicator                                |
| Warning triangle | `AlertTriangle`               | Warning / provisioning error                          |
| Search           | `Search`                      | Search input icon                                     |
| X                | `X`                           | Close / dismiss                                       |
| Chevrons         | `ChevronLeft`, `ChevronRight` | Pagination, navigation                                |
| Mail             | `Mail`                        | Admin invitation                                      |
| Refresh          | `RefreshCw`                   | Retry / resend                                        |
| Clock            | `Clock`                       | Countdown / pending                                   |
| Trash            | `Trash2`                      | Delete action                                         |
| Pause            | `Pause`                       | Suspend action                                        |
| Play             | `Play`                        | Activate action                                       |
| Palette          | `Palette`                     | Theme / branding                                      |
| Eye              | `Eye`                         | View details                                          |
| Shield           | `Shield`                      | Access control / security                             |
| Lock             | `Lock`                        | System role indicator                                 |
| Sparkles         | `Sparkles`                    | Wildcard permission (✦)                               |
| Filter           | `Filter`                      | ABAC FILTER effect                                    |
| Ban              | `Ban`                         | ABAC DENY effect                                      |
| GitBranch        | `GitBranch`                   | Condition tree nesting                                |
| Plus             | `Plus`                        | Add condition / add group                             |
| UserCog          | `UserCog`                     | Manage user roles                                     |
| MoreVertical     | `MoreVertical`                | Row action menu (⋮)                                   |
| LogIn            | `LogIn`                       | Sign In action                                        |
| LogOut           | `LogOut`                      | Sign Out action                                       |
| Timer            | `Timer`                       | Rate limit countdown                                  |
| ShieldAlert      | `ShieldAlert`                 | Super Admin login icon                                |
| Package          | `Package`                     | Plugin registry / archive                             |
| Upload           | `Upload`                      | Register plugin (manifest)                            |
| Settings         | `Settings`                    | Plugin configuration                                  |
| Activity         | `Activity`                    | Plugin health / metrics                               |
| ArrowUpCircle    | `ArrowUpCircle`               | Plugin update available                               |
| CircleDot        | `CircleDot`                   | Lifecycle step (current)                              |
| Circle           | `Circle`                      | Lifecycle step (future)                               |
| CheckCircle      | `CheckCircle`                 | Lifecycle step (complete)                             |
| XCircle          | `XCircle`                     | Lifecycle step (failed)                               |
| Power            | `Power`                       | Enable plugin action                                  |
| PowerOff         | `PowerOff`                    | Disable plugin action                                 |
| Home             | `Home`                        | Dashboard nav item                                    |
| User             | `User`                        | Profile nav item                                      |
| Menu             | `Menu`                        | Hamburger menu (mobile)                               |
| ChevronDown      | `ChevronDown`                 | Collapsible group expanded                            |
| ChevronRight     | `ChevronRight`                | Collapsible group collapsed                           |
| Bell             | `Bell`                        | Notification bell in header                           |
| Globe            | `Globe`                       | Language selector                                     |
| Key              | `Key`                         | Sign in action (session modal)                        |
| Languages        | `Languages`                   | Translation overrides nav item                        |
| Copy             | `Copy`                        | Copy key path (dev mode)                              |
| Pencil           | `Pencil`                      | Edit override indicator                               |
| FileText         | `FileText`                    | Translation file reference                            |
| Paperclip        | `Paperclip`                   | Attach file action                                    |
| Download         | `Download`                    | Download file action                                  |
| RotateCcw        | `RotateCcw`                   | Retry upload / retry job                              |
| Briefcase        | `Briefcase`                   | Deal icon (search results)                            |
| File             | `File`                        | Generic file icon                                     |
| FileUp           | `FileUp`                      | File uploading indicator                              |
| ListTodo         | `ListTodo`                    | Jobs nav item in admin sidebar                        |
| Share            | `Share2`                      | Share Plugin button, sharing empty state illustration |
| Unlink           | `Unlink`                      | Revoke sharing action                                 |
| RefreshCcw       | `RefreshCcw`                  | Retry loading plugin (error boundary)                 |
| ArrowLeft        | `ArrowLeft`                   | Go Back button (error boundary fallback)              |
| RotateCw         | `RotateCw`                    | Reload Page (root error boundary)                     |
| Folder tree      | `FolderTree`                  | Workspace hierarchy tree view                         |
| Move             | `Move`                        | Move workspace (reparent) action                      |
| Layout template  | `LayoutTemplate`              | Workspace template indicator                          |
| Bar chart        | `BarChart3`                   | Observability nav item (distinct from Activity)       |
| Gauge            | `Gauge`                       | Metrics tab icon (observability)                      |
| Route            | `Route`                       | Traces tab icon (observability)                       |
| Bell ring        | `BellRing`                    | Alerts tab icon (observability)                       |
| Heart pulse      | `HeartPulse`                  | Health tab icon (observability)                       |
| Puzzle (slot)    | `Puzzle`                      | Extension slot badge icon (reuse, already registered) |
| Eye off          | `EyeOff`                      | Hidden field empty state, visibility = hidden         |
| Chevron up       | `ChevronUp`                   | Move field up in layout config                        |
| Layout           | `Layout`                      | Layout Configuration nav item in admin sidebar        |
| Check            | `Check`                       | Editable toggle icon (✓ₑ)                             |
| Book open check  | `BookOpenCheck`               | Read-only toggle icon (✓ᵣ)                            |

---

## 11. Anti-Patterns

**Do NOT**:

- Use `confirm()` browser dialogs for destructive actions. Use `Dialog` component with clear consequences.
- Show raw JSON in user-facing UI (current issue in TenantDetailModal for settings/theme).
- Use color alone to indicate status — always pair with text label or icon.
- Use placeholder text as the only label for form inputs.
- Allow form submission with client-side-only validation (always validate server-side too).
- Create inline styles — use design tokens via Tailwind classes.

---

## 12. Cross-References

| Document                    | Path                                                            |
| --------------------------- | --------------------------------------------------------------- |
| UI component library        | `packages/ui/src/components/`                                   |
| Super Admin app             | `apps/super-admin/`                                             |
| Tenant web app              | `apps/web/`                                                     |
| Storybook stories           | `packages/ui/src/components/*/\*.stories.tsx`                   |
| Constitution (UX standards) | `.forge/constitution.md` Art. 1.3                               |
| Spec 011 Design Spec        | `.forge/specs/011-workspace-hierarchy-templates/design-spec.md` |
| Spec 012 Design Spec        | `.forge/specs/012-plugin-observability/design-spec.md`          |
| Spec 013 Design Spec        | `.forge/specs/013-extension-points/design-spec.md`              |
| Spec 014 Design Spec        | `.forge/specs/014-frontend-layout-engine/design-spec.md`        |
