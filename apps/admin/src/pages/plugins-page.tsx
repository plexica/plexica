// plugins-page.tsx — Plugin registry placeholder (S5-C01).

import { PlaceholderPage } from './placeholder-page.js';

export function PluginsPage(): JSX.Element {
  return <PlaceholderPage titleId="admin.nav.plugins" />;
}
