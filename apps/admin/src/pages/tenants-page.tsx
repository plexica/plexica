// tenants-page.tsx — Tenant list placeholder (S5-C01).

import { PlaceholderPage } from './placeholder-page.js';

export function TenantsPage(): JSX.Element {
  return <PlaceholderPage titleId="admin.nav.tenants" />;
}
