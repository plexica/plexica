// provision-page.tsx — Tenant provisioning placeholder (S5-C01).

import { PlaceholderPage } from './placeholder-page.js';

export function ProvisionPage(): JSX.Element {
  return <PlaceholderPage titleId="admin.nav.provision" />;
}
