# Plexica development stack. Included files keep every config below 200 lines.
# `docker compose up` remains the only command needed to start the full stack.
name: plexica-dev

include:
  - path: ./infra/compose/database-auth.yml
    project_directory: .
  - path: ./infra/compose/platform-services.yml
    project_directory: .
  - path: ./infra/compose/observability.yml
    project_directory: .
